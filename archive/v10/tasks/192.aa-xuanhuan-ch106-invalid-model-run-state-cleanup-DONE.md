# Task 192.aa DONE: xuanhuan Ch106 invalid model run-state cleanup

> **阶段**: V10.2 / Task 192 xuanhuan Ch200 climb
> **完成时间**: 2026-07-26
> **结论**: ✅ invalid model failure 痕迹已清理；run 恢复到 Ch105 可 resume

---

## Trigger

192.z 完成后恢复 Ch106→Ch125 时，shell 未显式设置 `LLM_MODEL`，LLM client 回退到默认 `deepseek-chat`。DeepSeek endpoint 返回：

```text
The supported API model names are deepseek-v4-pro or deepseek-v4-flash, but you passed deepseek-chat.
```

wrapper 被人工中止，避免继续污染更多章节。冻结目录：

```text
.tmp/backups/192aa_xuanhuan_ch106_invalid_model_failed_chapters_20260726-0812/
```

---

## Fix

确认 Ch106-Ch108 没有 accepted head / accepted version 后，使用 `ProjectRunRepository.update()` 清理 run state：

```json
{
  "before": {
    "current_chapter": 109,
    "failed_chapters": [106, 107, 108],
    "status": "running"
  },
  "after": {
    "current_chapter": 105,
    "failed_chapters": [],
    "status": "running"
  }
}
```

未修改任何正文、chapter version、gate 阈值或 harness 口径。

证据：

```text
.tmp/192aa_xuanhuan_ch106_invalid_model_status.json
.tmp/192aa_xuanhuan_ch106_run_state_cleanup.json
.tmp/backups/192aa_xuanhuan_ch106_invalid_model_failed_chapters_20260726-0812/
```

DB SHA256 after cleanup:

```text
A28A59EF06D0F93DBA33AC0CEF99BBA35CB9E96BA8106062D5D2072154CAC618
```

---

## Verification

Harness status:

```text
python scripts/run_v10_ch200_climb.py --status --genre xuanhuan --format json
```

Result:

```json
{
  "accepted": {
    "count": 105,
    "first": 1,
    "last": 105
  },
  "next_step": "run --audit --up-to <checkpoint> or --to <checkpoint>"
}
```

Script validation:

```text
python -m py_compile .tmp/apply_192aa_ch106_run_state_cleanup.py
ruff check .tmp/apply_192aa_ch106_run_state_cleanup.py
```

Result: PASS.

---

## Review

Manual review scope:

- Ch106-Ch108 had no accepted head, so clearing failed chapter list does not hide accepted-content defects.
- Cleanup used repository path and preserved completed chapters, accumulated summary, total cost, and run id.
- The root cause is external model configuration, not a gate or story-quality failure.
- Follow-up run must explicitly set `LLM_MODEL=deepseek/deepseek-v4-flash`.

Result: no P0/P1/P2 findings for this run-state cleanup.
