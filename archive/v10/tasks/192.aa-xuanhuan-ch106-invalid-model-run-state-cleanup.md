# Task 192.aa: xuanhuan Ch106 invalid model run-state cleanup

> **阶段**: V10.2 Task 192 xuanhuan Ch200 climb blocker
> **类型**: run-state cleanup / invalid model configuration recovery
> **优先级**: P0
> **依赖**: Task 192.z
> **状态**: ✅ 已完成

---

## 背景

192.z 完成后恢复 Ch106→Ch125 时，shell 未显式设置 `LLM_MODEL`，运行链路回退到默认 `deepseek-chat`。当前 DeepSeek endpoint 只接受 `deepseek-v4-pro` 或 `deepseek-v4-flash`，导致 Ch106-Ch108 的 GoalPlanner LLM call 均在进入正文生成前失败。

wrapper 已人工中止，避免继续污染更多章节。

冻结目录：

```text
.tmp/backups/192aa_xuanhuan_ch106_invalid_model_failed_chapters_20260726-0812/
```

当前事实：

- accepted heads: 105/105
- max accepted: Ch105
- Ch106-Ch108 无 accepted version
- `project_runs.current_chapter=109`
- `project_runs.failed_chapters=[106,107,108]`
- failure model: `deepseek-chat`
- correct V10 run model: `deepseek/deepseek-v4-flash`

---

## In Scope

- [x] 读取 frozen status 与 run DB。
- [x] 确认 Ch106-Ch108 没有 accepted head / accepted version。
- [x] 使用 repository path 将 run state 恢复到 Ch105 可 resume：`current_chapter=105`、`failed_chapters=[]`、`status=running`。
- [x] 显式使用 `LLM_MODEL=deepseek/deepseek-v4-flash` 重新 resume。
- [x] 修复完成前不得继续无显式模型的长跑。

---

## Out of Scope

- 不修改 LLM client 默认模型。
- 不修改 harness / pipeline on-failure 口径。
- 不改正文，不新增 chapter version。
- 不放宽任何 gate。

---

## 验收标准

- [x] run state 恢复为 accepted 105/105、failed=[]、next resume Ch106。
- [x] 清理前后证据落盘。
- [x] 后续 resume 命令显式设置 `LLM_MODEL=deepseek/deepseek-v4-flash`。
- [x] 产出 DONE 文档并同步必要入口。
- [x] 验证与 review 通过，提交一次，不 push。
