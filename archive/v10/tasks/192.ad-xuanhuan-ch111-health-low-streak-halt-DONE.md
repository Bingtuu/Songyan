# Task 192.ad DONE: xuanhuan Ch111 health_low_streak_halt repair

> **完成时间**: 2026-07-26  
> **父任务**: Task 192 xuanhuan Ch200 climb  
> **状态**: ✅ DONE  
> **范围**: Ch111 health_low_streak_halt + post-fix segment audit critical orphan 修复；未推进 Ch112/125

---

## 背景

Task 192.ac 完成后，使用 Task 191 harness 继续 xuanhuan Ch109→Ch125。Ch109-Ch111 均 accepted 并完成 settlement/summary，但 Ch111 后触发自动硬门：

```text
health_low_streak_halt: window=3 P2_total=11 >= limit=2
```

现场冻结：

```text
.tmp/backups/192ad_xuanhuan_ch111_health_low_streak_halt_20260726-0947/
```

wrapper:

```text
run-20260726-091909750
WRAPPER_RESULT=FAIL_NONZERO_EXIT
EXIT_CODE=1
```

---

## 初始事实

| 项 | 值 |
|----|----|
| accepted | 111/111 |
| failed | `[]` |
| run status | `paused` |
| Ch109 head | `v-d48df7a7` |
| Ch110 head | `v-6a691775` |
| Ch111 head | `v-229e33c0` |
| DB SHA256 | `7D807F8559A2BD1DC8DB2BCE804A06B1B3D0C8DB8D01A598C1CBEEBC054568DA` |

---

## 根因

`P2_total=11` 来自 Ch111 continuity report 的 overdue foreshadowings，而非新的 P1 hard critical：

- 11 条 foreshadowing 仍为 `status='overdue'`，其中部分 `lifecycle_status` 已是 `dormant` / `archived`，但 `list_overdue_unresolved()` 只排除 `status='resolved'`。
- 10 条对应 continuity human_marks 仍为 active P2。
- Ch111 修复第一阶段补充正文证据，将旧追杀/铜戒/令牌伏笔并入七日血阵主线。

第一阶段修复：

- parent: `v-229e33c0`
- new accepted/current head: `fix-111-6-334c5af5`
- evidence: `.tmp/192ad_xuanhuan_ch111_health_streak_recovery.json`

第二阶段复判时发现 segment audit @111 仍有 `critical_orphans=9`，原因是 9 条 critical tracking 未刷新到 next audit Ch114 所需的近端证据。

第二阶段修复：

- parent: `fix-111-6-334c5af5`
- final accepted/current head: `fix-111-7-4abf3d31`
- evidence: `.tmp/192ad_xuanhuan_ch111_segment_critical_recovery.json`

---

## 修复内容

使用 repository 路径完成：

- `ChapterVersionRepository.create()` 创建两个 Ch111 accepted patch。
- `ChapterHeadRepository.update()` 更新 Ch111 accepted/current head。
- `ForeshadowingRepository.update_status(..., "resolved")` 同步 11 条 overdue foreshadowings。
- `HumanMarkRepository.resolve()` soft-resolve 10 条 Ch111 P2 continuity marks。
- `SettingTrackingRepository.promote_to_active()` 刷新 9 条 critical tracking 到 `fix-111-7-4abf3d31`。
- `ProjectRunRepository.update()` 恢复 run 为 `running`、failed=[]。

Final DB state:

```json
{
  "head111": "fix-111-7-4abf3d31",
  "run_status": "running",
  "failed_chapters": [],
  "critical_orphans": [],
  "active_p2_marks_111": []
}
```

---

## 复判

Segment audit @111:

```json
{
  "critical_orphans": 0,
  "total_orphans": 84,
  "halt_would_fire": false
}
```

证据：`.tmp/192ad_xuanhuan_ch111_segment_audit_after.json`

T9 @111:

```json
{
  "meta_artifact": 0,
  "duplicate": 0,
  "timeline": 0,
  "accepted_count": 111,
  "up_to": 111,
  "valid": true
}
```

证据：`.tmp/192ad_xuanhuan_ch111_t9_after.json`

Harness status:

```json
{
  "accepted": {
    "count": 111,
    "first": 1,
    "last": 111
  }
}
```

DB SHA256 after:

```text
8C054F54E6CA2D99D082F2627D510D8CF96E9ACC3EAD30FC5DC18F9B5CF9E279
```

---

## 验证

```powershell
python -m py_compile .tmp/apply_192ad_ch111_health_streak_recovery.py
ruff check .tmp/apply_192ad_ch111_health_streak_recovery.py
python -m py_compile .tmp/apply_192ad_ch111_segment_critical_recovery.py
ruff check .tmp/apply_192ad_ch111_segment_critical_recovery.py
python scripts/segment_audit.py --db .tmp/task_v10_xuanhuan_ch200.db --project-id d160a55a51de4a2bb82440ebc03ec23a --up-to 111 --format json
songyan metrics --project-id d160a55a51de4a2bb82440ebc03ec23a --chapters 1-111 -o .tmp/192ad_xuanhuan_ch111_metrics.md
```

结果：

- script py_compile / ruff: PASS
- segment audit @111: PASS
- T9 @111: PASS
- run state: resumable (`running`, failed=[])

本任务未修改 `src/`、`tests/`、harness、five-gate、segment audit、CED、T9 或 health gate 判定函数，因此不触发全量 pytest、scifi 短窗口回归或 Task 189 baseline 重放。

---

## Review

- 数据源：SQLite 仍为唯一事实源；Ch111 accepted head、run state、marks、tracking、foreshadowing 状态均由 DB 核验。
- 版本链：创建 `fix-111-6-334c5af5` 与 `fix-111-7-4abf3d31`，未覆盖既有 accepted `v-229e33c0`。
- 状态一致性：run 恢复为 `current_chapter=111`、`status=running`、`failed=[]`。
- 审计口径：未放宽 `health_low_streak_halt`、segment audit、T9、five-gate 或 CED。
- 范围控制：未推进 Ch112/125；下一步仍需使用 Task 191 harness。

结论：未发现 P0/P1/P2 遗留问题。

---

## 后续

Task 192 可恢复推进 Ch112→Ch125。到 Ch125 后必须先执行 Task 191 harness 审计，并显式绑定：

```text
archive/v10/artifacts/189-scifi-ch200-baseline.json
```
