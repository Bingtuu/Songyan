# Task 192.p: scifi short regression ContextEmergency halt repair

> **阶段**: V10.2 / Task 192 xuanhuan Ch200 climb preflight
> **类型**: harness regression failure / runtime stability repair
> **优先级**: P1
> **状态**: ✅ 已完成（DONE：`tasks/192.p-scifi-short-regression-context-emergency-DONE.md`）

---

## Goal

修复或解释并复验 Task 192 工具链改动后触发的 scifi 短窗口回归失败，恢复 V10 守护项要求的 scifi 短窗口绿灯，再继续 xuanhuan clean Ch100 rebuild。

---

## Trigger

Task 192 为了防止 clean Ch100 source 被 failed chapter 污染，修改 `scripts/run_172b_ch100_climb.py`：

- `RUN_ID=192` 默认 `ON_FAILURE=abort`；
- `abort/retry` 策略下若 `ProjectRunResult.chapters_failed` 非空，分段 climb 立即停止；
- 旧 `172b/172c` 默认仍保持 `isolate`。

按 V10 守护项，harness / 运行工具改动后执行 scifi 短窗口回归：

```powershell
$env:SONGYAN_FORCE_EXIT='1'
$env:SONGYAN_RUN_COST_BUDGET='20.0'
$env:LLM_MODEL='deepseek/deepseek-v4-flash'
powershell -File scripts/run_with_timeout.ps1 -TimeoutSec 7200 -- python scripts/run_172a7_genre_validation.py --templates scifi --end 10
```

结果失败：

- wrapper: `WRAPPER_RESULT=FAIL_NONZERO_EXIT`, `EXIT_CODE=1`
- `.tmp/task172a7_validation.json`: `scifi` error 为连续 3 章 ContextEmergency 伴随失败或质量异常（Ch6-Ch9）
- stdout 摘要：
  - `AutoHalt attempt 1/3`: Ch3-Ch5
  - `AutoHalt attempt 2/3`: Ch5-Ch7
- 关键现场：
  - run_id: `run-90ba3294`
  - project_id: `0dde7289f1aa4a49a0b3d7c442278a1f`
  - DB: `C:\Users\Admin\AppData\Local\Temp\task172a7_scifi_wb7lskce\songyan.db`
  - DB SHA256: `C086484314C3BC275B84EE322C2F108E5EE47DD68A646C23CFE4C4B0C3534C90`

冻结现场：

- `.tmp/backups/192_scifi_short_regression_failed_20260725-120940/`
- 含 wrapper out/err/meta、`.tmp/task172a7_validation.json`、`songyan.db`

---

## Initial Assessment

本失败不是 `scripts/run_172b_ch100_climb.py` 直接调用路径导致：回归命令使用的是 `scripts/run_172a7_genre_validation.py`，其 `run_project_pipeline(..., on_failure="isolate")` 语义未被本次代码改动改变。

但 V10 守护项要求 harness / 质量工具改动后 scifi 短窗口回归必须执行；当前回归未通过，因此 Task 192 工具链改动不能收口，也不能继续启动新的 xuanhuan clean Ch100 source 终判。

---

## In Scope

- [x] 读取冻结 DB 与日志，确认最终 accepted / failed / paused 状态。
- [x] 判定失败根因：
  - DeepSeek v4/flash 输出波动导致的偶发 settlement / quality failure；
  - scifi 短窗口当前 profile / ContextEmergency 策略在 Ch6-Ch9 的真实退化；
  - `run_172a7_genre_validation.py` retry/resume 语义是否把已恢复样本仍判为失败；
  - 其他近期代码改动引发的回归。
- [x] 若为工具判定缺陷，修工具并补测试。
- [x] 若为真实运行稳定性问题，做最小机制修复，不放宽 V10 gate。
- [x] 修复后重新执行 scifi 短窗口回归，必须得到可接受证据。
- [ ] 修复后再回到 Task 192 clean Ch100 rebuild：重新 `RUN_ID=192 TEMPLATE_ID=xuanhuan --init`，不得复用污染 DB。

---

## Out of Scope

- 不放宽 ContextEmergency / AutoHalt 阈值来掩盖失败。
- 不把失败样本作为 scifi 回归通过证据。
- 不修改 CED / T9 / five-gate 口径。
- 不启动 xuanhuan Ch200 或其它体裁 Ch200。

---

## Acceptance

- [x] 根因有日志/DB 证据，不只基于猜测。
- [x] 必要代码修复有聚焦测试。
- [x] `python -m pytest tests/ -q` 通过。
- [x] `ruff check src/ tests/` 通过。
- [x] scifi 短窗口回归重新执行并通过，或形成明确的用户决策点。
- [x] 若涉及 harness / Ch200 口径，再补对应 baseline replay 要求（本任务未改 Ch200 harness / five-gate / segment audit 口径，无需重放 Ch200 baseline）。
- [x] 产出 `tasks/192.p-scifi-short-regression-context-emergency-DONE.md` 后，Task 192 主线才可继续。
