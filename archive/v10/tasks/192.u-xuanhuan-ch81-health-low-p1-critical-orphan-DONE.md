# Task 192.u DONE: xuanhuan Ch81 health_low_p1 critical orphan repair

> **阶段**: V10.2 / Task 192 xuanhuan clean Ch100 rebuild
> **完成时间**: 2026-07-26
> **结论**: ✅ 已完成，Ch81 hard gate 恢复 PASS，可回到 Task 192 继续 Ch100

---

## Trigger

Ch76-Ch81 生成完成后，Ch81 触发 hard gate：

```text
health_low_p1_halt: P1_count=1 (critical orphaned setting)
```

现场：

- DB: `.tmp/task172b_xuanhuan_ch100.db`
- project_id: `d160a55a51de4a2bb82440ebc03ec23a`
- run_id: `run-2f42e276`
- wrapper: `run-20260726-011647464`
- accepted heads: 81/81
- failed: `[]`
- P1 target: `xuanhuan_lingyuan.technique.lingyuan_quan_first_form`
- segment audit @81: `critical_orphans=10` / `halt_would_fire=true`

冻结目录：

```text
.tmp/backups/192u_xuanhuan_ch81_health_low_p1_halt_20260726-0202/
```

---

## Root Cause

1. Ch81 hard gate 的直接原因是 continuity auditor 在 Ch81 生成 P1 mark：

```text
设定 '灵渊《灵渊拳》第一式' 自第75章后已 6 章未被提及，本章必须回收或提及。
```

2. Segment audit @81 预测下一审计点 Ch84，发现 10 条 active critical setting 的 `last_mentioned_chapter` 太旧：

```json
{
  "critical_orphans": 10,
  "total_orphans": 110,
  "halt_would_fire": true
}
```

3. `scripts/run_172b_ch100_climb.py` 对所有 run 默认 `HALT_RETRIES=2`，导致 Task 192 在 gate paused 后自动 retry/resume 到 Ch82，违反 V10 硬门失败后冻结纪律。

---

## Fix

### Ch81 continuity recovery

创建版本化 accepted 修复版本：

- parent: `v-df18b9ed`
- new accepted: `fix-81-5-214e4cd7`

补入一小段 continuity reference，不改变剧情结果：

```text
第三份令牌、母亲留下的血脉封印、守门者关于猎渊者的警告，以及灵渊空间深处那条金色封印锁链，都被这一句话串到了一起。

刚才刺破铜印屏障时，陆沉也终于确认另一件事：《灵渊拳》第一式的发力结构没有外放成拳影，而是被他压进了短剑的剑势里。那道淡金色气劲与石棺表面的金色兽形图腾同源，像是令牌碎片、血脉烙印和传承拳法在同一瞬间咬合。
```

使用 existing repository path 完成：

- `ChapterVersionRepository.create()`
- `ChapterHeadRepository.update()`
- `SettingTrackingRepository.promote_to_active()`
- `HumanMarkRepository.resolve()`

证据：

```text
.tmp/192u_xuanhuan_ch81_continuity_recovery.json
```

### Harness discipline

修复 `scripts/run_172b_ch100_climb.py`：

```python
HALT_RETRIES = int(os.getenv("HALT_RETRIES", "0" if RUN_ID == "192" else "2"))
```

Task 192 默认 hard gate 后不自动 retry/resume；历史 172b/172c 默认仍保持 2 次重试。

---

## Verification

### DB

- accepted heads: 81/81
- Ch81 accepted/current head: `fix-81-5-214e4cd7`
- unresolved active P1 marks: `[]`
- 修复后 DB SHA256:

```text
FA551AE2067CA0DBFCB3FFAD831C8B550B55BA97018E032FDDA24075229DD5F9
```

### Segment Audit

`.tmp/192u_xuanhuan_ch81_segment_audit_after.json`:

```json
{
  "critical_orphans": 0,
  "total_orphans": 100,
  "halt_would_fire": false
}
```

### T9

`.tmp/192u_xuanhuan_ch81_t9_after.json`:

```json
{
  "meta_artifact": 0,
  "duplicate": 0,
  "timeline": 0,
  "accepted_count": 81,
  "up_to": 81,
  "valid": true
}
```

### Five-Gate

`.tmp/192u_xuanhuan_ch81_five_gate_after.json`:

```text
PASS
```

### Focused Tests

```powershell
python -m pytest tests/test_172cs_wuxia_health_calibration.py::test_task192_ch100_rebuild_defaults_to_abort_failure_policy tests/test_172cs_wuxia_health_calibration.py::test_ch100_climb_legacy_default_remains_isolate -q
```

Result:

```text
2 passed
```

### Ruff

```powershell
ruff check src/ tests/ scripts/run_172b_ch100_climb.py
```

Result:

```text
All checks passed
```

### Full Pytest

```powershell
powershell -File scripts/run_with_timeout.ps1 -TimeoutSec 2400 -- python -m pytest tests/ -q
```

Result:

```text
3006 passed, 2 skipped, 1 xfailed, 7 warnings
WRAPPER_RESULT=PASS_NORMAL_EXIT
```

Wrapper:

```text
run-20260726-021612840
```

### Scifi Short Regression

```powershell
$env:CHECKPOINTER_MODE='sqlite'
$env:SONGYAN_FORCE_EXIT='1'
$env:LLM_MODEL='deepseek/deepseek-v4-flash'
powershell -File scripts/run_with_timeout.ps1 -TimeoutSec 7200 -- python scripts/run_172a7_genre_validation.py --templates scifi --end 10
```

Result:

```text
Ch1-Ch10 completed
failed=[]
WRAPPER_RESULT=PASS_NORMAL_EXIT
```

Wrapper:

```text
run-20260726-023003613
```

### Code Review

`bits-code-guard` diff-only review:

- Scope: `scripts/run_172b_ch100_climb.py`
- Result: no P0/P1/P2 findings
- Report: `.tmp/bits-code-guard-192u/report.html`

---

## Next

回到 Task 192，继续 xuanhuan clean Ch100 rebuild。下一次启动仍使用：

```powershell
$env:TEMPLATE_ID='xuanhuan'
$env:RUN_ID='192'
$env:CHECKPOINTER_MODE='sqlite'
$env:SONGYAN_FORCE_EXIT='1'
$env:SONGYAN_RUN_COST_BUDGET='20.0'
$env:LLM_MODEL='deepseek/deepseek-v4-flash'
powershell -File scripts/run_with_timeout.ps1 -TimeoutSec 14400 -- python scripts/run_172b_ch100_climb.py --to 100
```

Task 192 的默认 `HALT_RETRIES=0` 已生效；若再触发 hard gate，应立即停在现场，不再自动越界推进。
