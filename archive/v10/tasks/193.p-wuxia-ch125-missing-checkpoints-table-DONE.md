# Task 193.p DONE: wuxia Ch125 missing checkpoints table

> **状态**: ✅ 完成
> **任务书**: `tasks/193.p-wuxia-ch125-missing-checkpoints-table.md`
> **完成时间**: 2026-07-28
> **父任务**: Task 193 wuxia Ch200 climb

---

## 结论

Task 193.p 已完成。旧 Ch100 source DB 复制出的 V10 target DB 缺少 LangGraph `checkpoints` / `writes` tables，导致 `prune_orphan_checkpoints()` 在 graph setup 前直接 `DELETE FROM checkpoints` 崩溃。修复后，缺表场景幂等返回 0；表存在时原有按 `metadata.project_id` 清理孤儿 checkpoint 的行为保持不变。

---

## 修复内容

- 修改 `src/songyan/workflows/checkpointer.py`：
  - sqlite 模式下先查询 `sqlite_master`；
  - `checkpoints` 表不存在时返回 `0`；
  - 表存在时继续执行原清理逻辑。
- 修改 `tests/test_153_run_level_resume.py`：
  - 新增 `test_prune_orphan_checkpoints_sqlite_without_tables`，覆盖旧 continuation DB 尚未创建 checkpoint tables 的场景。

---

## 验证

| 命令 / 证据 | 结果 |
|-------------|------|
| `python -m pytest tests/test_153_run_level_resume.py::TestCheckpointPruning -q` | `3 passed` |
| `ruff check src/songyan/workflows/checkpointer.py tests/test_153_run_level_resume.py` | `All checks passed` |
| `powershell -File scripts/run_with_timeout.ps1 -TimeoutSec 2400 -- python scripts/run_v10_ch200_climb.py --to 125 --genre wuxia` | 原 missing `checkpoints` failure 未复发；进入 Ch101+ 正常生成链路 |
| resume 证据 | `project_pipeline.pruned_orphan_checkpoints ... pruned_count=0` 后进入 Ch101；第二轮 resume 后 `pruned_count=167`，继续正常 |

---

## 当前父任务进展

Task 193 主长跑仍在进行中，未到 Ch125 审计点。当前 authoritative state：

| 项 | 值 |
|----|----|
| target DB | `.tmp/task_v10_wuxia_ch200.db` |
| run_id | `run-v10-wuxia-5bbfab3a` |
| accepted | 114/125 target segment |
| current_chapter | 115 |
| failed | `[]` |
| total_cost | `2.238771` |
| latest accepted | Ch114 `v-2b41977f` |
| latest wrapper | `run-20260728-062641505-20260728-062641505` timeout at Ch115 pre-generation |

下一步继续使用 Task 191 harness resume：

```powershell
powershell -File scripts/run_with_timeout.ps1 -TimeoutSec 2400 -- python scripts/run_v10_ch200_climb.py --to 125 --genre wuxia
```
