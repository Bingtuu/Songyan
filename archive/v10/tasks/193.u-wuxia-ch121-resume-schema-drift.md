# Task 193.u: wuxia Ch121 resume schema drift（pause_reason 列缺失）修复

> **阶段**: V10.2 Task 193 wuxia Ch200 climb blocker
> **类型**: harness schema 迁移修复（193.r 后续撞墙）
> **优先级**: P0
> **依赖**: Task 193.r
> **状态**: ◐ 修复已验证，实跑复核中

---

## 背景

193.r 完成后首次 resume Ch121→Ch125：

```powershell
LLM_MODEL='deepseek/deepseek-v4-flash'
powershell -File scripts/run_with_timeout.ps1 -TimeoutSec 7200 -- python scripts/run_v10_ch200_climb.py --to 125 --genre wuxia --cost-budget 4.0
```

wrapper:

```text
run-20260728-115044132
WRAPPER_RESULT=FAIL_NONZERO_EXIT
EXIT_CODE=1
```

resume 正常进入（completed_count=120、resume_start=121、prune orphan checkpoints=85），但 `_save_run_state` 在 `ProjectRunRepository.update` 处崩溃：

```text
sqlite3.OperationalError: no such column: pause_reason
```

根因：wuxia target DB 复制自 172b 时代 source，无 193.r 新增的 `pause_reason` 列；harness `--to` 路径不经 CLI 的 `init_schema`，无任何环节对既有 DB 应用 additive 迁移。与 193.p（旧库缺 checkpoint tables）同类：harness 需对 source 复制库的 schema drift 负责。

失败未造成数据损坏：update 失败即中止，run 行保持 paused@121，未进入章节生成；prune_orphan_checkpoints 清理的 85 条为孤儿 checkpoint（193.p 语义内的常规 housekeeping）。

---

## In Scope

- [x] harness `--to` 真实执行前调用 `ensure_target_schema(db_path)`（幂等 `init_schema`），覆盖 pause_reason 及未来 additive 迁移列。
- [x] 单元测试：旧 schema 库迁移后列存在；二次调用幂等（先 RED 后 GREEN）。
- [ ] 重新 resume Ch121→Ch125 实跑复核。

## Out of Scope

- 不改 repo 写入 SQL 兼容无列旧库（迁移是正解，双写兼容是债）。
- 不动 wuxia run 数据（run 行保持 paused@121，resume 流程自行恢复）。
- audit 只读路径不加迁移（detect_halt 已有 `_column_exists` 保守回退）。

---

## 验收标准

- [x] 新增测试全绿；`tests/test_193r_eval_caliber_fixes.py` + `tests/test_191_ch200_harness.py` 37 passed；ruff 全绿。
- [ ] Ch121→Ch125 resume 实跑通过 schema 点，进入正常章节生成。
- [ ] 产出 `tasks/193.u-wuxia-ch121-resume-schema-drift-DONE.md`，同步 `docs/STATUS.md`、`tasks/V10-README.md`、`README.md`。
