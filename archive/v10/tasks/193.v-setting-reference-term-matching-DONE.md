# Task 193.v DONE: setting tracking 正文引用词条匹配修复（《》拆分 / 虚字归一化 / 名词边界放宽）

> **阶段**: V10.2 Task 193 wuxia Ch200 climb 配套根因治理（193.s Phase C 承接）
> **类型**: 运行时匹配层修复（有界；已执行 E 组回归）
> **状态**: ✅ 已完成

---

## 修复结果（`src/songyan/agents/settlement_extractor/_apply.py`）

### F1 · 词条生成缺陷

- **F1a**：`_setting_reference_terms` 的 name 拆分正则与 `_SETTING_REFERENCE_SPLIT_RE` 均补入书名号 `《》〈〉`；name 拆分逻辑抽为 `_setting_name_reference_terms()`（供 F3 判定 name 派生 term）。
- **F1b（设计调整，相对任务书）**：core phrase 下限**按来源分级**——name 派生 5 → 3（`灵渊拳`/`第一式`/`守门者` 级短核心词成为 term），**description 派生维持 5**。调整原因：TDD 过程中既有护栏 `test_task138c_aliases_do_not_match_broad_words` RED，证明 description 的 3-4 字泛词（`巨型遗迹` ↔ `英仙臂外侧巨型遗迹`）误刷面不可接受；分级后诊断 8 章覆盖不变。
- **F1c**：多 token 兜底 `_has_multi_token_setting_reference` 随 name 派生短词扩容恢复部分效用（`守门`/`门者`/`守门者` n-gram），阈值未放宽（仍 ≥3 token 且含 ≥3 字 token）。

### F2 · 虚字插字归一化

- 新增 `_strip_reference_particles()`（删除 的/之/了）与 `_term_in_content_relaxed()`，仅 `_detect_setting_references` 路径启用；`与守灵交易` ↔ `与守灵的交易` 类插字 paraphrase 可命中。
- **共享 `_term_in_content` 默认行为零变化**（continuity auditor `_scanners.py:163` 消费者不受影响，有显式测试守护）。

### F3 · 设定名词边界放宽

- `_term_in_content_relaxed(allow_cjk_suffix=True)` 仅对 **name 派生 ≥3 字 term** 放开 CJK 后缀：`猎渊者` ↔ `猎渊者印记`、`血灵宗` ↔ `血灵宗执事`。2 字 term 不适用（`灵渊兽` 不命中 `灵渊`，有测试守护）；description 派生 phrase 维持现行边界。

### F4 · 误报率对照（`.tmp/193v_refresh_diff.py`，复用 git HEAD 旧逻辑精确回放）

| 库 | active settings | 回放章 | 新增命中 | 旧有命中丢失 | 诊断 8 章捕获 |
|----|----------------|--------|---------|-------------|--------------|
| xuanhuan Ch200 | 186 | 200 | 964 | **0** | **7/8** |
| wuxia Ch125 | 118 | 125 | 385 | **0** | n/a（案例均在 xuanhuan） |

- 落盘：`.tmp/193v_refresh_diff_xuanhuan.json` / `.tmp/193v_refresh_diff_wuxia.json`。
- 诊断 8 章明细：`guardian_hunter_deception@93`、`lingyuan_quan_first_form@{104,146,149,150}`、`mother_descendant@{120,166}` 全部捕获；**唯一未捕获 `mother_descendant@163`**——`你母亲把它种进你体内` 类深度 paraphrase，词面不可达，归入 alias 遗留（与诊断 §5.2 保守估计 ≥88% 一致，实际 7/8=87.5%）。
- 诊断修正一条：`mother_descendant@120` 旧逻辑经多 token 兜底本就能捕获，非生产漏报——192.ae 硬门真因是 Ch117/118 isolate 空洞章导致 last_mentioned 停滞，词面匹配无过失。
- 误刷抽检：xuanhuan top8 + wuxia top5 新增命中全部为主线核心名词（宗门名/功法名/地名/族群名/传承名）出现的真实场景，无可拒收误刷；松动类别（`血灵宗`/`青岩镇`/`断刀门`/`追风堂` 等核心名词命中即刷新同簇设定）已评估为可接受——命中均有 ≥3 字 + name 派生 + low-info 过滤三重约束。

## 验证证据

| 验证 | 结果 |
|------|------|
| `tests/test_193v_setting_reference_terms.py` | **14 passed**（先 RED 后 GREEN；含共享函数行为不变守护、比喻句误刷红线） |
| `tests/test_task137_setting_recycling.py`（既有护栏 53 项） | **全绿**（含 test_138c 泛词护栏、test_171s 复合设定、test_172bp 引号名） |
| settlement/continuity 相邻套件 | **254 passed, 1 xfailed** |
| 全量 `python -m pytest tests/ -q` | **3049 passed, 2 skipped, 1 xfailed**（496s） |
| `ruff check src/ tests/` | **All checks passed** |
| `mypy src/` | **Success: no issues found in 176 source files** |
| xuanhuan 冻结库 five-gate @200（显式 189 baseline） | **PASS**，halt=None（与 192 DONE 一致） |
| xuanhuan 冻结库 segment audit @200（`--genre xuanhuan`） | **critical_orphans=0、total=50、halt_would_fire=false**（与 192 DONE 一致） |
| scifi end10 回归（E 组） | **通过**：首跑 9/10（Ch2 `database is locked` 瞬时锁失败——与全量 pytest 并发争用，非逻辑问题），同库同 run 单章 resume 补齐 Ch2 `v-c44810df`；终态 **accepted 1-10 完整链、failed=[]、T9=0（meta/duplicate/timeline 全 0，persist=False 复算）、overdue=0、budget 峰值 0.9918、0 halt、总成本 0.954**；wrapper PASS_NORMAL_EXIT |

## 行为切换点（留痕）

- wuxia run `run-v10-wuxia-5bbfab3a`：Ch1-125 为旧匹配逻辑，**Ch126+ 起为新逻辑**；历史 `last_mentioned` 不回填。
- xuanhuan Ch200 终判样本全程为旧逻辑 + 人工 patch，V10 总验收（Task 195）跨体裁对比时须注明三体裁匹配逻辑口径差异。

## 遗留

- alias / 命名漂移机制（`血引归墟`↔`血祭刀法`/`逆血归元`、`mother_descendant@163` 类深度 paraphrase）：路由 V11。
- settlement JSON 持久化（诊断 §1.3）：另立任务候选。

## 验收对照

- [x] F1/F2/F3 实现且验收用例全绿；共享 `_term_in_content` 默认行为不变。
- [x] F4 刷新差异清单落盘：漏报捕获 7/8（≥70% 门槛），抽检无不可接受误刷。
- [x] scifi end10 回归通过；xuanhuan 冻结库复跑结论一致。
- [x] 全量 pytest、ruff、mypy 通过。
- [x] 行为切换点已记录（本节 + V10-README）。
- [x] 产出本 DONE 文档，同步 `docs/STATUS.md`、`tasks/V10-README.md`、`README.md`、`AGENTS.md`。
