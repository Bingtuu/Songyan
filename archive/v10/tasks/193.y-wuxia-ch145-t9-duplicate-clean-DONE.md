# Task 193.y DONE: wuxia Ch145 T9 duplicate clean

> **阶段**: V10.2 / Task 193 wuxia Ch200 爬坡
> **完成时间**: 2026-07-29
> **结论**: ✅ 已完成，Ch150 边界恢复 T9=0，可回到 Task 193 继续 Ch175

---

## Trigger

Task 193.x 修复后复判 Ch150 段边界 T9 时发现 duplicate=1（段跑 Ch126→Ch150 产物自带，非 193.x 刷新引入——193.x 只动 `setting_tracking`，不动正文）：

```json
{
  "meta_artifact": 0,
  "duplicate": 1,
  "timeline": 0,
  "accepted_count": 150,
  "up_to": 150,
  "valid": false
}
```

命中 Ch145 accepted `v-f581c63b`：第 85 段逐字重复第 30 段（similarity=1.0）：

```text
"因为盟主府的书房，只有两个人能进。"铁心兰竖起两根手指，"莫长空，和他的贴身护卫。那枚令从我手里过的时候，封蜡上有莫长空的私印。我看过。"
```

上下文：Ch145 为"战斗中闪回"结构，para 83-85 是 para 28-30 对话的再现（83 为近似改写、84 短问句未达检测阈值、85 逐字重复被命中）。

---

## Frozen Evidence

冻结目录（与 193.x 同目录，clean 前 DB 另存）：

```text
.tmp/backups/193x_wuxia_ch150_segment_audit_critical_orphans_20260729/task_v10_wuxia_ch200_before_193y.db
```

clean 前 DB SHA256（193.x 刷新后 / 193.y clean 前）：

```text
3659B108C7E39B6B78480EFF91C077EC82EB682107F4B66C87043598557C9655
```

duplicate 定位证据：`.tmp/193y_t9_dup.txt`。

---

## Fix

使用已有 `songyan.services.text_cleanliness_cleaner.apply_chapter_text_cleaning()` 对 Ch145 执行版本化 deterministic clean（脚本 `.tmp/193y_clean_ch145.py`，一次性不入库）：

- original accepted version: `v-f581c63b`
- cleaned accepted version: `clean-145-6-c534f0e7`
- chapter_heads Ch145 current/accepted: `clean-145-6-c534f0e7`
- remaining hard issues: `[]`
- action evidence: `.tmp/193y_wuxia_ch145_duplicate_clean.json`

该服务在事务内创建新 `chapter_versions` 记录并更新 `chapter_heads`，未覆盖原 accepted 正文，旧版本可追溯。clean 仅移除检测命中的第 85 段（102 段 -> 101 段），无其他正文改动。

---

## Verification

### T9（clean 后，复用 harness `_t9_counts_from_rows` 口径）

`.tmp/193x_wuxia_ch150_t9_after.json`:

```json
{
  "up_to": 150,
  "accepted_count": 150,
  "meta_artifact": 0,
  "duplicate": 0,
  "timeline": 0,
  "valid": true
}
```

### Segment Audit（clean 后）

`.tmp/193y_wuxia_ch150_segment_audit_after.json`:

```text
critical_orphans=0
halt_would_fire=false
```

### Five-Gate（clean 后，显式绑定 Task 189 baseline）

`.tmp/193y_wuxia_ch150_five_gate_after.json`:

```text
PASS
```

### Tests

本任务未修改源码，只执行 DB 版本化正文清理与文档更新；未运行全量 pytest/ruff。风险匹配验证为 Ch150 T9、segment audit、five-gate 三项段边界复核。

---

## Remaining Notes

- clean 移除第 85 段（闪回中重复的回答句）后，para 84 问句"你怎么确定是莫长空？"无显式回答，直接接战斗打断段。叙事上可读作"闪回被蒙面人攻击打断"，属轻微叙事跳跃，不阻塞 T9/segment/five-gate；若后续 Ch175+ 审计或人工阅读判定需要补笔，应另开 193 后缀任务走 accepted patch 路径。
- Ch145 段内 para 83-84 为 para 28-29 的近似/短句复现，未达 `detect_duplicate_paragraphs` 阈值，不在 T9 命中范围。
- 可以回到 Task 193，继续 wuxia `--to 175`；Ch175 边界仍必须重复 T9/five-gate（绑 Task 189 baseline）/segment audit 纪律。
