# Task 194.a: urban Ch123 settlement JSON parse failure

> **父任务**: Task 194 urban Ch200 爬坡  
> **类型**: 段边界前硬失败修复 / settlement JSON parse / accepted gap  
> **状态**: ◐ 进行中  
> **冻结时间**: 2026-07-30 07:56  

---

## 背景

Task 194 使用 Task 191 harness 从 urban Ch100 source 推进 Ch101→Ch125：

```powershell
$env:LLM_MODEL='deepseek/deepseek-v4-flash'
powershell -File scripts/run_with_timeout.ps1 -TimeoutSec 21600 -- python scripts/run_v10_ch200_climb.py --to 125 --genre urban --cost-budget 8
```

wrapper 正常退出，但 run 结果不是 PASS：

- `final_status=partial`
- `failed=[123]`
- completed 缺 Ch123
- harness status 显示 accepted count=124、last=125

按 V10 goal 与 Task 194 段边界纪律，Ch125 不能审计通过，也不能继续 Ch126。

---

## 冻结现场

冻结目录：

```text
.tmp/backups/194a_urban_ch125_failed_gap_20260730-0756/
```

包含：

- target DB copy：`task_v10_urban_ch200.db`
- wrapper log/meta：`wrapper.out.log` / `wrapper.meta.txt`
- harness status：`status.json`
- git status：`git_status.txt`
- DB SHA256：`db.sha256.txt`
- DB inspect：`inspect_194a_ch123.json`

---

## 已确认事实

DB inspect 结论：

- Ch122/124/125 均有 accepted/current head。
- Ch123 `chapter_heads`：
  - `current_version_id='v-123-1-7b4f52c2'`
  - `accepted_version_id=NULL`
  - `status='draft'`
- Ch123 只有 draft `v-123-1-7b4f52c2` 与已 abandoned revision `rev-123-2-b454e20c`。
- Ch123 summary count=0。
- `project_runs.failed_chapters='[123]'`，`completed_chapters` 缺 123。

wrapper log 结论：

- Ch123 quality gate 已进入 `human_gate.decision decision=accept`。
- SettlementExtractor 随后失败：

```text
settlement_extractor_node.settlement_failed_needs_review
error='LLM 返回内容无法解析为 JSON（标准解析和 repair 均失败）'
```

因此根因是 Ch123 settlement JSON parse failure，不是正文质量 gate 失败。

---

## 修复目标

1. 补齐 Ch123 settlement / accepted head / summary。
2. 恢复 run `run-v10-urban-743a979a`：
   - completed Ch1-Ch125 连续无 gap；
   - `failed=[]`；
   - `status` 至少回到可审计状态。
3. 重跑 Ch125 checkpoint 审计，必须显式绑定 Task 189 baseline：

```powershell
python scripts/run_v10_ch200_climb.py --audit --genre urban --up-to 125 --baseline archive/v10/artifacts/189-scifi-ch200-baseline.json --format json
```

4. 只有 Ch125 five-gate PASS、segment PASS、T9=0 后，才允许回到 Task 194 主线继续 Ch126→Ch150。

---

## 约束

- 不修改 five-gate / segment audit / T9 / CED 判定函数。
- 不把优秀度或结构 spike 混入本修复。
- 不覆盖 Ch124/Ch125 accepted head。
- 若选择手工 DB 修复，必须创建新 accepted `chapter_versions` 记录或使用 repository `accept_version()`，禁止覆盖 Ch123 draft 内容。
- 修复后必须产出 `tasks/194.a-urban-ch123-settlement-json-parse-DONE.md`。

