# Task 194.g: urban Ch175 T9 meta hard gate

> **父任务**: Task 194 urban Ch200 爬坡  
> **类型**: T9 hard gate / meta artifact clean / checkpoint Ch175  
> **状态**: ◐ 进行中  
> **冻结时间**: 2026-07-30 15:42  

---

## 背景

194.f 修复后，使用 Task 191 harness resume 到 Ch175：

```powershell
powershell -File scripts/run_with_timeout.ps1 -TimeoutSec 7200 -- python scripts/run_v10_ch200_climb.py --to 175 --genre urban --cost-budget 16 --format json
```

Ch175 generation PASS，run completed 1..175，failed=[]，Ch175 accepted/current head：

```text
v-2f8b36fe
```

随后执行 Ch175 checkpoint audit：

```powershell
python scripts/run_v10_ch200_climb.py --audit --up-to 175 --genre urban --baseline archive/v10/artifacts/189-scifi-ch200-baseline.json --format json
```

结果：

- five-gate PASS
- segment audit PASS (`critical_orphans=0`, `halt_would_fire=false`)
- T9 hard gate FAIL：meta=2，duplicate=0，timeline=2 report-only

---

## 冻结现场

冻结目录：

```text
.tmp/backups/194g_urban_ch175_t9_meta_hard_gate_20260730-1542/
```

包含：

- target DB copy：`task_v10_urban_ch200.db`
- wrapper log：`wrapper.out.log` / `wrapper.err.log`
- wrapper meta：`wrapper.meta.txt`
- Ch175 audit artifacts：`five_gate.json` / `segment_audit.json` / `metrics.md`
- harness status：`status.json`
- run/head snapshot：`run_state.json`
- T9 evidence：`t9.json`
- DB SHA256：`db.sha256.txt`

Run state：

- run_id：`run-v10-urban-743a979a`
- status：`completed`
- current_chapter：175
- completed：1..175
- failed：`[]`
- total_cost：10.710629

---

## T9 证据

T9 result：

```text
passed=false
measured=meta=2, duplicate=0, timeline=2
threshold=meta=0; duplicate=0; timeline report-only
detail=元标记违规章: [171, 175]；时间线诊断章: [129, 137]
```

Hard hits：

| Chapter | version | artifact | evidence |
|---------|---------|----------|----------|
| Ch171 | `v-5ac30ced` | protected directive leak | `【保护内容 — 请勿修改】` |
| Ch175 | `v-2f8b36fe` | ellipsis placeholder paragraph | pure paragraph `...` |

Timeline hits Ch129/Ch137 are report-only and do not block PASS.

---

## 修复目标

1. 对 Ch171 / Ch175 创建版本化 clean accepted/current heads，清除 T9 meta artifacts。
2. 不改剧情事实，不做整章重写。
3. 重建对应章节 RAG chunks。
4. 复跑 T9，要求 meta=0、duplicate=0；timeline 仍 report-only。
5. 复核 Ch175 checkpoint：accepted=175/175、failed=[]、five-gate PASS、segment PASS、T9=0。

---

## 约束

- T9 是硬红线，不接受解释性豁免。
- 每次正文修复必须新增 `chapter_versions` 记录，禁止覆盖。
- 修复前不得继续 Ch176+。
