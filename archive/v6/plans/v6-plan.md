# V6 阶段规划：叙事骨架 MVP + 长篇质量度量 + 可靠长跑底盘

> 前置：V5（V5.0/V5.1/V5.2）已完整完成并验收。本规划基于 `docs/300-chapter-gap-analysis.md` 的代码级 gap 分析。
> 编写时间：2026-06-30（第二轮修订：新增叙事骨架阶段为治本起点）。
> 任务编号沿用 V5 的连续编号惯例（V5 收尾于 Task 140），本阶段从 **Task 141** 起连续编号（Task 141-159）。

---

## 1. V6 的定位与边界

### 1.1 一句话目标

> **V6 给系统装上一根最小可用的叙事骨架（治本起点），同步建立长篇质量度量，并补足无人值守长跑底盘，验证到 Ch100-150。**
> V6 不承诺"完整高质量 300 章"，那是 V7 的目标（见 `docs/v7-vision.md`）。

> **修订说明**：初版 V6 把"orphan 源头限流"当作治本动作。第二轮代码核查（GoalPlanner 只看上一章 120 字、models 无任何前瞻结构、无 plan→generate→re-plan 闭环）确认：orphan 累积、文学质量无指标，都是**缺自顶向下叙事架构**这一根因的下游症状。因此本轮把"建最小叙事骨架"提为阶段 0，置于度量与末端治理之前。详见 `docs/300-chapter-gap-analysis.md` §1。

### 1.2 为什么 300 章不放进 V6

1. **gap 体量不对等**：叙事骨架 MVP + 度量 + 长跑底盘已是一个阶段的体量；完整"线索经济闭环"和"文学质量自动修复"是半开放问题，不会在一个阶段内收敛。
2. **V5 历史规律**：每一次长窗口实跑都撞出新墙（Ch5→Ch8→Ch18→Ch115→Ch15 enforce）。当前治理管线只验证到 Ch30，直接跳 Ch300 违背全部历史经验。
3. **指标自欺风险**：health 8.5 对 P3 麻木，掩盖了 orphan 总量仍在涨。**V6 必须先让指标说真话，且先有可度量的主线对象。**

### 1.3 V6 完成的判定（阶段验收）

V6 通过 = 同时满足以下五项（所有阈值与术语见 §1.4，由不在场的人也能独立判真假）：
- **N（骨架）**：项目可携带全书大纲 / 弧规划；GoalPlanner 输入包含弧上下文（可在 `context_snapshots` 验证）；Ch1-Ch50 内至少一条「主线线索」完成 §1.4-T1 定义的可追溯状态跃迁；
- **D（度量）**：orphan 绝对量、新 critical 产生速率、质量债、文学趋势、弧级伏笔兑现率五类长期指标已入库且可在 `songyan report`/`songyan metrics` 查看；
- **S（源头收敛）**：满足 §1.4-T6 的 orphan 斜率下降 + 归因判据（用「新 critical 产生速率下降」证明非靠录入丢弃）；
- **R（可靠）**：单条命令无人值守完成 **Ch1-Ch100**（"完成"见 §1.4-T2），中途人为 kill 后可 run 级 resume 续完，无需人工改命令/清理；
- **V（验证）**：在 V5.2 + 骨架管线上取得 **Ch1-Ch150** 连续运行证据（非旧 `run-a2bed648`），且全程满足 §1.4 的红线（T3/T4/T5）。

### 1.4 验收阈值与术语定义（基线标定口径）

> 下列为**首版阈值**。其中标 ⚙ 的需在 V6 启动后用 138n/138k 历史 DB 复算校准（阶段 A 出口产出标定报告后冻结）；标 ✓ 的为结构性/二值判据，立即生效。本节是 V6 全部验收标准的唯一阈值出处。

| ID | 术语 / 判据 | 定义（可判定口径） |
|----|------------|--------------------|
| **T1** ✓ | 主线线索「可追溯状态跃迁」 | `ArcPlan` 标注为 `is_mainline=true` 的 `PlotThread`，在弧窗口内发生 `opened→advanced` 或 `advanced→resolved` 状态变更，且每次变更可在 report 中定位到具体章节号与 source_version_id |
| **T2** ✓ | 「完成 / 跑通」 N 章 | 目标区间每章都产出 `accepted` 或 `edited` head（`degraded_accept` 计入"完成"但同时计入质量债 T4）；`failed` 或未解决的 `human_review_required` 均算未完成 |
| **T3** ⚙ | 文学趋势「红线」 | 任一文学维度（character_autonomy / conceptual_grounding 等；对应 `literary_observations` 列 `character_autonomy_score` / `conceptual_grounding_score` / `fissure_preservation_score`，注意 `conceptual_idling` 是 observation 类型而非列）的滑动窗口（W=5 章）均值，相对该 run 前 10 章基线下降 **≥20%** 即触红线 |
| **T4** ⚙ | 质量债「红线」 | 任一连续 50 章窗口内：`degraded_accept` 占比 **≤20%** 且 `convergence_failed` 占比 **≤10%**；超出即触红线 |
| **T5** ⚙ | DB / 性能「红线」 | Ch100 时 DB 文件 **≤300MB**（150 章基线 196MB）；单次连续性扫描查询耗时 **≤ 基线 1.5×** |
| **T6** ⚙ | orphan 斜率下降 + 归因 | (a) Ch50-Ch100 窗口 orphan 绝对总量线性斜率 **≤ 138n 基线斜率 ×0.5**；(b) P1(critical) orphan 全程 **=0**；(c) 归因：「每章新 critical 产生速率」(T7) 相对 138k 基线下降幅度 **≥ orphan 斜率下降幅度的 50%**，且因录入降级被丢弃/降级的 critical 设定数 **≤ 同窗口新增 critical 总数的 15%** |
| **T7** ✓ | 每章新 critical 产生速率 | 每章 settlement 写入的 `category=critical` 新设定条数；统计口径为「写入侧」，与 orphan（"沉寂侧"）区分，用于 T6 归因 |
| **T8** ⚙ | 趋势窗口 N | 文学/指标"连续下滑"检测窗口 **N=5 章**（与现有 gate `streak_window=3` 区分：gate 看审计点，趋势看章） |

> 阈值校准纪律：⚙ 阈值在阶段 A 出口用历史 DB 复算实际分布后，若发现首版值明显不合理（如 138n 基线本身已超红线），须在标定报告中记录并调整，再冻结为 V6 正式口径——**不允许在长跑撞红线后临时放宽**。
>
> **阶段 A 标定冻结（2026-07-01，Task 148z，见 `archive/v6/reports/v6-stageA-threshold-calibration.md`）**：
> - **T3 冻结**：W=5 均值相对前 10 章基线降 ≥20%（a2bed648 历史 run 触线、30 章健康 run 不触线，口径有效）。
> - **T6(a) 冻结**：Ch50-100 窗 orphan 斜率 ≤ **3.14/章**（=138n 基线 6.2836×0.5）；**T6(b)** P1 critical orphan=0；**T6c** T7 基线=1.767/章（138k rehearsal），降幅比值手工核算，"被降级 critical ≤15%" 子句延后至 Task 149。
> - **T8 冻结**：N=5。
> - **T4 / T5 延后实测**：degraded/convergence 历史不可得（仅 qg_false，≈0-3%）→ 延后至 Task 157 首窗；DB/性能红线延后至长跑（Task 156/158，干净 150ch 基线 ≈196MB 待重测）。
> - **T5 实测（Task 158, run-10d7961b）**：Ch100 DB 尺寸 84.78MB，未超 300MB 红线；连续性扫描耗时前 10 样本基线 89.1ms，Ch50/Ch70 分别达到 172ms/157ms（1.93×/1.76×）破线。按阈值校准纪律，T5 未冻结；Task 159 需复核基线窗口与 1.5× 系数后再冻结。

---

## 2. 工作流（沿用 Ralph Loop）

每个 Task 遵循：测试 → 发现 → 定位 → 更新文档 → 修复 → 验证。长跑验证用隔离副本 DB，结论以 `*-DONE.md` + `docs/reports/` 报告为准。任务命名沿用 RX 后缀法管理迭代轮次。

执行总原则（来自 gap 分析 §9 修订版）：**结构先行 → 度量同步 → 末端治理 → 工程加固 → 长窗口验证**。后一组依赖前一组，不可并行抢跑。

> 关键纪律：阶段 0（骨架）与阶段 A（度量）可同期启动，因为度量需要"主线对象"才能定义弧级指标；但**末端治理（阶段 B）必须在骨架 + 度量都落地后**，否则无法判定"orphan 下降是骨架之功还是录入丢弃的假象"。

---

## 3. Task 拆分路线图

> **代码核实修正（2026-07-01，Task 141-144 文档创建时回写）**：创建阶段 0 任务文档前对主干代码做了核实，发现本规划初稿有 4 处假设与真实代码不符，据实修正如下（详细以各 `tasks/14x-*.md` 为准）：
> 1. **`songyan report` 当前不读 continuity DB**（`cli/main.py` `report_cmd` 只读 JSONL run logs 生成流式验证报告）。因此阶段 A（Task 145-148）"在 songyan report 暴露度量"需先建立"report 读取 DB 逐章度量"的能力——这是阶段 A 的**真实前置 gap**，非纯展示改动。
> 2. **LiteraryAuditor 已入库**：`literary_observations` 表已有 `character_autonomy_score` / `conceptual_grounding_score` / `fissure_preservation_score` 列。故 Task 147 是"加趋势查询 + 滑动窗口"，非"从零入库"，工作量小于初稿描述。注意维度名是 **`conceptual_grounding_score`**，`conceptual_idling` 仅是 observation type（存于 observations JSON），非独立列。
> 3. **continuity_reports 无逐章计数列**：该表只存 JSON blob（orphaned_settings/forgotten_items 等），orphan 计数是运行时 `classify_report` 派生。故 Task 145 画"逐章曲线"需新增逐章度量的持久化设计（时间序列或逐章 continuity 记录），不能假设已有计数列。
> 4. **命名冲突**：已存在 `OpenThread`（context.py，settlement 运行时提取的开放线索，无状态机）。V6 新增的 `PlotThread`（前置规划线索，有 `opened/advanced/resolved` 状态机）必须独立共存、不合并；`ArcPlan`（前置规划）亦区别于回顾型 `ArcSummary`。详见 `archive/v6/tasks/141-narrative-skeleton-data-model.md` 的「关键区分」表。

### 阶段 0：最小叙事骨架 MVP（治本起点）

> 目标：把章节目标从"反应式只看上一章 120 字"升级为"自顶向下从全书大纲 / 弧规划派生"，并建立最小的"线索开启-兑现"追踪。这是减少 orphan 与让文学质量可度量的共同前提。
> 边界：MVP 不追求完整线索经济（那是 V7）。先做"骨架存在、能注入、能追踪"，不做"自动重规划闭环"。

| Task | 名称 | 目标 | 验收标准 |
|------|------|------|----------|
| **141** | 叙事骨架数据模型 | 新增 `StoryOutline`（全书核心冲突/主线）+ `ArcPlan`（弧目标/应开启与收束的线索）+ `PlotThread`（线索：开启章、预期收束弧、状态）数据模型与表 | 模型 + 迁移 + repository；单测覆盖 CRUD 与生命周期；不破坏现有 schema |
| **142** | 项目创建可携带大纲 | `create-project` 支持录入/导入全书大纲与弧规划（可选，缺省回退现状） | 创建带大纲的项目；缺省项目行为不变；大纲可被后续节点读取 |
| **143** | GoalPlanner 自顶向下派生 | GoalPlanner 输入增加"当前弧目标 + 本弧未收束线索 + 临近兑现伏笔"，章节目标从骨架派生 | prompt 注入弧/线索上下文；新旧对比下章节目标与弧目标一致；单测覆盖派生逻辑；无大纲时回退旧行为 |
| **144** | 线索经济约束（MVP） | CreativeDirector / Writer 约束注入"本章应推进/收束的线索；非必要不开新线"；PlotThread 状态随 settlement 更新 | 线索 `opened/advanced/resolved` 状态被追踪并可在 report 计数；Ch1-Ch20 小窗口复跑中，每章新 critical 产生速率（T7）相对 138k 同区间基线下降 **≥30%**；单测覆盖线索状态机 |

**阶段 0 出口**：Ch1-Ch20 复跑中——(a) ≥90% 章节目标可在 report 追溯到某条 `ArcPlan`；(b) 至少一条主线线索完成 T1 的完整 `opened→advanced→resolved` 跃迁；(c) 每章新 critical 产生速率（T7）较 138k 基线下降 ≥30%。

---

### 阶段 A：度量同步（让指标说真话 + 让骨架可判定）

> 目标：把"被掩盖的退化"暴露出来，并为骨架效果提供判据。与阶段 0 同期推进。
> **前置 gap（代码核实）**：`songyan report` 现只读 JSONL run logs，不读 continuity DB。阶段 A 第一步须先建立"report 从 DB 读取逐章度量"的能力，否则 Task 145-148 的曲线无处展示。此前置工作计入 Task 145。

| Task | 名称 | 目标 | 验收标准 |
|------|------|------|----------|
| **145** | orphan 绝对量 + 新 critical 产生速率监控 | 在 ContinuityReport / `songyan report`/`songyan metrics` 暴露 orphan **绝对总数**及分类分布的逐章曲线（与 health_score 解耦），并新增「每章新 critical 产生速率」（T7，写入侧）曲线 | 复跑 138n 数据可还原出 orphan 总量斜率与 T7 速率两条独立曲线；report 输出 orphan 总数/critical/P3 + T7 共四条曲线；单测覆盖两类统计口径 |
| **146** | 质量债账本 | 跨章累计 `degraded_accept` / `convergence_failed` / QG=false 章数，持久化到 run 级表 | run 结束可查"降级接受章列表 + 各占比"；report 新增质量债汇总段并按 T4 口径标红线；单测覆盖累计 |
| **147** | 文学质量趋势化 | LiteraryAuditor 维度**已入库**（`literary_observations` 表已有 `character_autonomy_score` / `conceptual_grounding_score` / `fissure_preservation_score`）；本 Task 加按章节范围回读 + 滑动窗口（W=5）趋势查询 | 新增趋势查询；可按 T3/T8 口径检出"连续 N=5 章某维度均值下滑 ≥20%"；不改变 accept 流程（仍只诊断） |
| **148** | 弧级伏笔兑现率 + 长程伏笔台账 | 基于阶段 0 的 PlotThread/ArcPlan，统计弧级伏笔兑现率；暴露长程未兑现伏笔 (source, expected, 跨度, 状态)，标记被"逾期归档"而非真兑现的伏笔 | report 可列弧级兑现率与"被系统遗忘"伏笔数；单测覆盖 overdue/archived 区分 |

**阶段 A 出口**：用现有 138n / 138k 历史 DB 跑一遍新度量，复现"orphan 总量持续上涨""质量债累积"等被旧指标掩盖的现象；**并产出 §1.4 标定报告**——用历史分布校准全部 ⚙ 阈值（T3/T4/T5/T6/T8）并冻结为 V6 正式口径。**这一步必须先于任何末端治理。**

---

### 阶段 B：末端治理（缓解症状）

> 目标：在骨架已减少 orphan 产生的基础上，把治理端的硬上限与误分类一并修掉。依赖阶段 0+A 的判据。

| Task | 名称 | 目标 | 验收标准 |
|------|------|------|----------|
| **149** | 录入侧降级（非硬丢弃） | 单章超额 `new_settings`（尤其 critical）标记为候选/低优先而非直接入库或丢弃，避免事实库与正文脱节 | 单章 critical 录入超 §1.4 阈值时转候选（不丢弃）；复跑 138k 同项目 orphan 斜率下降且被降级 critical 数 ≤ 新增 critical 总数 15%（T6c）；单测验证候选可回升为正式 |
| **150** | `_infer_setting_category` 收紧 | 收紧 critical 判定，要求关键词 + 主线相关性双命中；去除硬编码主角名，改读 project 主角档案 | 138m 中 22 个"世界观细节被误判 critical"样本不再判 critical（≥20/22）；单测；主角名不再硬编码 |
| **151** | MR 上限自适应 + 相关性排序 | 实现 138m 推荐但未落地的随活跃量自适应上限；排序改为"主线相关性 + 沉默"综合（复用阶段 0 的线索结构） | MR 上限随活跃 critical 数自适应（非恒定 12）；Ch100 处 `rule_auditor.mandatory_reference_missing` 命中率较 138n 不升高；单测覆盖排序与上限 |
| **152** | critical 显式 resolve/作废出口 | 为 critical 设定提供"剧情交代已 resolve"或"标记废弃"真回收路径，替代沉寂归档 | 显式 resolve 设定不再计入 orphan；废弃设定移出 MR 注入；单测覆盖生命周期 |

**阶段 B 出口**：Ch1-Ch50 复跑满足 §1.4-T6 全部三项——orphan 斜率 ≤138n×0.5、P1=0、归因判据（T6c）成立，证明下降主因是"新设定产生减少"而非"录入丢弃"；同时 health ≥ 7.0。

---

### 阶段 C：工程加固（无人值守长跑）

> 目标：让 30 小时连续运行不因单点故障整批 abort。

| Task | 名称 | 目标 | 验收标准 |
|------|------|------|----------|
| **153** | run 级断点续跑 | 持久化 run 进度，崩溃后用同一命令真正 resume；in-flight 章节状态可恢复，不从头重算整批 | 模拟中途 kill 后 resume，已 accepted 章跳过、in-flight 章正确恢复；孤儿 checkpoint 被清理 |
| **154** | LLM 限流感知与全局预算 | 增加 429/Retry-After 识别与退避；单 run 全局 LLM 调用预算 + 熔断 | 注入 429 的集成测试下不会级联 abort；超预算触发可观测熔断而非静默卡死 |
| **155** | 失败隔离策略 | 默认 `on_failure` 从 abort 改为"隔离单章、继续后续、最后汇总失败清单" | 单章硬失败不终止整批；run 结束输出失败章清单可供定点重跑 |
| **156** | 运行中 DB 维护 | 增量 VACUUM / wal_checkpoint 在长跑中按周期执行 | Ch100 长跑满足 §1.4-T5（DB ≤300MB、扫描查询 ≤基线 1.5×）；新增维护调度单测 |

**阶段 C 出口**：单命令无人值守跑通 Ch1-Ch100，中途人为注入崩溃可 resume 续完，无需手工干预。

---

### 阶段 D：长窗口验证

> 目标：在 V5.2 + 骨架 + B + C 的真实管线上取得 Ch150 证据。

| Task | 名称 | 目标 | 验收标准 |
|------|------|------|----------|
| **157** | Ch1-Ch50 集成验证 | 阶段 0+A+B+C 改动合入后的首个完整窗口 rehearsal | 50/50 完成（T2）；满足阶段 B 出口（T6）；主线线索 T1 跃迁可追溯；全程不触 T3（文学）/T4（质量债）红线；报告入库 |
| **158** | Ch1-Ch100 长跑验证 | 无人值守 + run 级续跑实跑 | 100/100 完成（T2）；中途至少一次 kill→resume 成功；orphan 总量 / T7 速率 / 质量债 / 文学趋势 / 弧级兑现率五类曲线全程可读，且全程不触 T3/T4/T5/T6 任一红线 |
| **159** | Ch1-Ch150 治理管线复现 | 在 V5.2 + 骨架管线上复现 V5.1 的 150 章里程碑（替代旧 `run-a2bed648`） | 150/150 完成（T2）；与旧基线逐项对比：orphan 斜率不高于 138n、P1=0、文学趋势无 T3 红线、质量债无 T4 红线、≥1 条主线线索全程可追溯；产出 V6 验收报告并逐条核对 §1.3 的 N/D/S/R/V |

**阶段 D 出口 = V6 阶段验收**：满足 §1.3 的 N/D/S/R/V 五项，每项均按 §1.4 阈值（T1–T8）逐条核对，写入验收报告。

---

## 4. 依赖关系与节奏

```
阶段0（骨架 141-144）┐
                     ├─► B（末端治理 149-152）─► D 部分（157 Ch50）
A（度量 145-148）─────┘                              │
                     └─────► C（工程 153-156）──────►┴─► D（158 Ch100, 159 Ch150）
```

- 阶段 0 与 A 同期启动（度量需要骨架提供的主线对象来定义弧级指标）。
- A 出口要用历史 DB 验证度量可信；B 必须在骨架 + 度量落地后，才能判定 orphan 下降的真实归因。
- C 可与 B 部分并行（不同代码域），但 158/159 长跑必须在骨架 + B + C 都合入后。
- 每个阶段出口未达标则不进入下一阶段，避免债务滚动。

---

## 5. V6 明确不做（划归 V7）

| 项 | 归属 | 理由 |
|----|------|------|
| 满 Ch300 渐进验证 | V7 | 需 V6 底盘与度量先稳定 |
| enforce 门禁默认化 | V7 | 需质量债账本与自适应门禁先到位（V6 只做度量与自适应预研） |
| 题材泛化（数值 allowlist / 设定簇外置为 genre 配置） | V7（可选） | 仅当目标含多题材；与 300 章主线正交 |
| 文学质量的"闭环修复"（不止诊断、能驱动改写） | V7 | V6 只做趋势度量，闭环属高风险开放问题 |

---

## 6. 风险与对策

| 风险 | 对策 |
|------|------|
| **骨架 MVP 范围膨胀**：线索经济很容易做成大系统 | 严守 MVP 边界——只做"骨架存在、能注入、能追踪"，不做自动重规划闭环（留 V7）；无大纲项目必须能回退旧行为 |
| **GoalPlanner 注入弧上下文后 prompt 膨胀 / 行为漂移** | 用 138k/138n 同项目做新旧章节目标对比，确认派生目标与弧规划一致且不劣化正文质量 |
| 阶段 A 度量暴露的退化比预期严重，B 无法在窗口内收敛 | 接受"分阶段降低斜率"，以斜率下降幅度而非绝对清零为出口 |
| 录入侧降级误伤真实关键设定 | 降级为候选 + 可观测，配合小窗口复跑校准，不直接丢弃 |
| run 级续跑与 LangGraph checkpoint 语义冲突 | 优先做 run 级进度表（项目自管），checkpoint 仅作单章内恢复 |
| Ch100/150 长跑耗时（>10h）拖慢迭代 | 先用历史 DB + 小窗口快速验证逻辑，长跑仅在阶段出口执行 |

---

## 7. 与现有文档的关系

- 论证基础：`docs/300-chapter-gap-analysis.md`
- 下一阶段：`docs/v7-vision.md`
- 事实入口：V6 启动后应新建 `tasks/V6-README.md` 作为 Task 141 系列事实入口，并在 `docs/STATUS.md`、`docs/INDEX.md` 登记。
