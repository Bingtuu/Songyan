# Task 187.v: urban Ch3 numerical settlement isolate 复核

> **阶段**: V9.5 urban Ch100 爬坡撞墙复核
> **父任务**: Task 187 urban Ch100 爬坡执行
> **关联任务**: Task 187.s health 校准 clean rerun
> **类型**: settlement numerical validation / documented isolate / clean rerun
> **优先级**: P1（阻断 187.s clean rerun）
> **状态**: ✅ 完成（documented isolate；fresh rerun 未复发）
> **现场 DB**: `.tmp/task172b_urban_ch100.db`
> **现场备份**: `.tmp/187v_diagnostic_ch3_numerical_settlement_fail.db`
> **project_id**: `150964ac662e468db3322814ca92b9d2`
> **run_id**: `run-2aee202e`

---

## 触发条件

Task 187.s 修改 urban `continuity.health_overdue_weight=0.08` 后，重新初始化 clean urban DB 并执行 Ch1-Ch25。运行在 Ch3 settlement 阶段失败：

```text
settlement.validation_failed
errors=[
  "角色 char-a2bff064 的 bank_balance closing_value (197.32) 不等于 公式值 (-4602.680)"
]
project_pipeline.chapter_failed chapter_number=3
```

中断时 DB 状态：

- Ch1 accepted
- Ch2 accepted
- Ch3 draft，`accepted_version_id = NULL`
- Ch4 draft（已启动但被人工中断，不作为证据）
- `project_runs.failed_chapters = [3]`
- `budget_used_peak = 0.7547`
- `context_emergency_count = 0`

---

## 设计 review 结论（2026-07-21）

结论：**不改代码，作为 documented isolate 处理并重新 clean rerun**。

理由：

1. `bank_balance` 是真实台账字段，不是 telemetry snapshot 或无证据读数类字段。
2. 现有规则要求 `numerical_update.closing_value` 必须等于公式值；这是 AGENTS 硬约束，不能为单次 LLM 抽取失败放宽。
3. 现有测试已明确覆盖真实 ledger formula error 仍应 hard fail：
   - `test_task138d_r2_real_resource_ledger_formula_error_still_fails`
   - `test_generic_quantity_still_requires_real_ledger_formula`
   - `test_task3t_real_ledger_formula_error_still_fails`
   - `test_task138l_real_ledger_with_telemetry_formula_still_validated`
4. 本次 failure 发生在 187.s clean rerun 早期，和 health profile 权重无因果关系。

因此本任务的“开发”动作是：保留现有 numerical validation，不做代码变更；冻结现场后重新 clean rerun。若同类 `bank_balance` 或真实台账 formula mismatch 在 fresh rerun 中再次阻断，则升级为新的机制修复任务，不能在本任务中偷改硬校验。

---

## 执行记录

### A. 现场冻结

已生成诊断备份：

```text
.tmp/187v_diagnostic_ch3_numerical_settlement_fail.db
```

备份方式：对 `.tmp/task172b_urban_ch100.db` 执行 SQLite `VACUUM INTO`，避免后续 `--init` 覆盖唯一现场。

### B. 复核结论

不修改：

- `src/songyan/agents/settlement_extractor/_validate.py` numerical formula hard check；
- `tests/test_settlement_extractor.py` 中真实 ledger formula error 用例；
- five-gate / CED / T9 / health 判定口径。

### C. 下一步

已重新初始化 clean urban DB 并完成 Ch25 正式样本：

- DB：`.tmp/task172b_urban_ch100.db`
- project_id：`81e345042b124ee2a73094b82e4be555`
- run_id：`run-d22b1a44`
- Ch1-Ch25：25/25 accepted
- failed_chapters：`[]`
- Ch3 numerical settlement formula mismatch 未复发
- Ch25 five-gate：PASS
- T9：meta/artifact=0、duplicate=0、timeline=0

结论：本次 Ch3 `bank_balance` mismatch 保持 documented isolate 处理，不改 numerical formula hard check。本任务完成。

---

## 非目标

- 不把 Ch3 formula mismatch 解释为通过；
- 不过滤真实台账字段的 formula mismatch；
- 不在当前诊断 DB 上继续跑到 Ch25；
- 不让 Ch4 draft 进入任何验收证据。
