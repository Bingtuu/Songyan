content: |
  ## 角色对抗性目标锚定（Task 170k）

  本章每个出场人类角色都有来自 CreativeDirector 的 `[对抗性目标: 角色ID]` 条目。写作时必须：
  - 每个主要人类角色的出场必须服务于其 `goal` 或 `fear`，不能仅为递信息、解释设定或附和主角；
  - 对白的差异化应来自角色目标/恐惧与主角目标的冲突，而不是来自贴标签式声纹；
  - 高概念信息应通过角色目标冲突、失败或代价自然释放，禁止用主角内心独白或非人实体独白直接总结；
  - 如果一个角色的目标/恐惧在本章无法体现，则减少其出场或让其行动带来意外后果。
