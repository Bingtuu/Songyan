# Task 172b.p — xuanhuan Ch100 伏笔 overdue 长窗口修复

> **状态**: 🔄 进行中  
> **归属**: V8.2 / 172b 撞墙定点修复  
> **触发点**: xuanhuan Ch100 爬坡在 Ch65 early-warning 读数触发 V 维度 overdue FAIL  
> **原则**: 不放宽 V 维度口径，不改 `vdim_compare.py`，只修运行时伏笔生命周期参数

---

## 1. 现象

172b xuanhuan 实跑到 **Ch65/100** 后主动停跑冻结现场：

| gate | Ch65 xuanhuan | sci-fi 同章尺度 | verdict |
|---|---:|---:|---|
| budget | 0.981 | 0.989 | PASS |
| CED/1k | 10.41 | 9.39 × 1.15 | PASS |
| overdue | **171** | **126** | **FAIL** |
| health | 9.4 | >=8.0 | PASS |
| completeness | 65/65 | 65/65 | PASS |

这不是 Ch100 终判，但已经超过 sci-fi 同章尺度，并接近/超过 sci-fi Ch100 基线（168），按 `tasks/172b-xuanhuan-ch100-climb.md` §3/§4 纪律停止爬坡并路由 172b.p。

## 2. 根因

诊断脚本 `.tmp/diagnose_172bp_overdue.py` 对 live DB `.tmp/task172b_xuanhuan_ch100.db`（project `1e7ce627...`）的 Ch65 冻结现场统计：

| 指标 | xuanhuan Ch65 | sci-fi Ch65 对照 |
|---|---:|---:|
| planted <= Ch65 | 191 | 143 |
| resolved | 0 | 0 |
| 平均 planted / chapter | **2.94** | **2.20** |
| horizon 主峰 | +12（168/191） | +1/+2/+3 |
| overdue @Ch65 | **171** | 129（vdim 插值口径约 126） |

结论：

1. **不是 resolved 记账链在 xuanhuan 独坏**：sci-fi 对照同样 resolved=0，V7 基线本身主要靠较低 plant 密度 + 既有 due/overdue 口径通过。
2. **172a.p floor=12 确实生效**：168/191 条 horizon 正好是 +12；短窗口 S 维度因此过关。
3. **floor=12 是短窗口修复，不足以覆盖 Ch100 长窗口**：xuanhuan plant 密度比 sci-fi 高约 34%，在无 resolved 的现有 MVP 架构下，overdue 必然线性追上并超过 sci-fi 同章尺度。

因此 172b.p 的最小修复是把 xuanhuan 的 **Ch100 长窗口 horizon floor** 从 12 提升到 48。模拟当前 Ch65 已 planted 数据：

| floor | overdue@Ch65 | overdue@Ch100（仅当前已 planted） |
|---:|---:|---:|
| 12 | 172 | 191 |
| 24 | 135 | 191 |
| 36 | 91 | 191 |
| **48** | **50** | **167** |
| 50 | 41 | 163 |

`floor=48` 是满足 Ch100 `overdue <= sci-fi Ch100 168` 的最小安全档之一；后续 Ch66+ 新 planted 伏笔在 Ch100 前不会变 overdue，因此不会继续抬高 Ch100 overdue。

## 3. 修复范围

### 代码

- `src/songyan/db/genre_runtime_profile_repo.py`
  - xuanhuan `foreshadowing_horizon_floor`: `12 -> 48`
  - scifi 保持 `0`，保证无 profile/旧行为回退不变
  - wuxia 保持 `12`，172c 仍按已有短窗口情报启动；若 wuxia Ch100 出现同类问题再单独 172c.p

### 测试

- 更新 `tests/test_172ap_foreshadowing_horizon_floor.py`
  - xuanhuan registry 断言改为 `48`
  - 新增/保留 clamp 纯函数测试，证明 `floor=48` 只抬高、不缩短
  - scifi floor=0 no-op 不变
  - wuxia floor=12 不变

### live DB 修复

对 `.tmp/task172b_xuanhuan_ch100.db` 中 live project 的未 resolved xuanhuan 伏笔执行一次性修复：

```sql
UPDATE foreshadowings
SET expected_resolve_chapter = planted_in_chapter + 48,
    status = 'planted'
WHERE project_id = '1e7ce6279b224e7f8e476f6f4e963417'
  AND status != 'resolved'
  AND expected_resolve_chapter IS NOT NULL
  AND expected_resolve_chapter < planted_in_chapter + 48;
```

说明：

- 只抬高 horizon，语义与 172a.p `_clamp_foreshadowing_horizon` 一致。
- 重置 `status='planted'` 是因为 due/overdue 状态由 `expected_resolve_chapter` 派生；expected 被抬到未来后，旧 overdue 状态已过期。
- 不修改 sci-fi 基线 DB，不修改 `vdim_compare.py`，不降低验收门槛。

## 4. 验收

1. `python -m pytest tests/test_172ap_foreshadowing_horizon_floor.py -q`
2. `python -m pytest tests/test_172a3_runtime_profile_injection.py tests/test_172a5_gate_threshold_profile.py tests/test_172a6_character_decay_profile.py tests/test_172a_genre_runtime_profile.py tests/genres/ -q`
3. `ruff check src/songyan/db/genre_runtime_profile_repo.py tests/test_172ap_foreshadowing_horizon_floor.py`
4. live DB 修复后：
   - `python .tmp/vdim_compare.py 65` 必须五门 PASS
   - `python .tmp/segment_audit.py 65` 显示 next audit critical_orphans=0
5. resume 172b climb 到 Ch75 / Ch100，终判仍用 `vdim_compare.py`，不能因本修复放宽任何 gate。

### 4.1 当前执行结果（2026-07-15）

代码与测试：

- `xuanhuan.foreshadowing_horizon_floor`: `12 -> 48`
- scifi 保持 `0`，wuxia 保持 `12`
- `python -m pytest tests/test_172ap_foreshadowing_horizon_floor.py -q` → **12 passed**
- `python -m pytest tests/test_172a3_runtime_profile_injection.py tests/test_172a5_gate_threshold_profile.py tests/test_172a6_character_decay_profile.py tests/test_172a_genre_runtime_profile.py tests/genres/ -q` → **164 passed**
- `ruff check src/songyan/db/genre_runtime_profile_repo.py src/songyan/models/genre_runtime_profile.py tests/test_172ap_foreshadowing_horizon_floor.py` → **All checks passed**

live DB 修复：

- 已备份：`.tmp/task172b_xuanhuan_ch100.pre172bp.db`
- 修复脚本：`.tmp/repair_172bp_overdue_floor.py`
- 修复行数：188
- overdue@Ch65: **171 -> 50**
- overdue@Ch100（仅当前已 planted）: **188 -> 166**（低于 sci-fi Ch100 基线 168）

修复后 Ch65 gate：

| gate | 修复后 Ch65 | verdict |
|---|---:|---|
| budget | 0.981 | PASS |
| CED/1k | 10.41（<= 9.39 × 1.15） | PASS |
| overdue | 50（< sci-fi 126） | PASS |
| health | 9.4 | PASS |
| completeness | 65/65 | PASS |

`segment_audit.py 65`：next continuity audit @Ch66 `critical_orphans=0`，halt would NOT fire。

## 5. 后续

若 Ch100 达标：

- 172b 完成，V 维度闭合。
- 按 V8-README 纪律，再写 172c wuxia Ch100 任务文档并启动第二体裁爬坡。

若 Ch100 仍因 overdue FAIL：

- 不继续抬评估 tolerance。
- 评估是否进入主动回收调度（超出当前 MVP，需要新 task 论证）。
