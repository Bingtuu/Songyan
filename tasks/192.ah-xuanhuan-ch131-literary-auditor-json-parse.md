# Task 192.ah — xuanhuan Ch131 LiteraryAuditor JSON parse failure

> **状态**: ◐ 待修复  
> **父任务**: `tasks/192-xuanhuan-ch200-climb.md`  
> **触发时间**: 2026-07-26  
> **前置任务**: `tasks/192.ag-xuanhuan-ch129-segment-audit-critical-orphans-DONE.md`  
> **阻塞范围**: Task 192 xuanhuan Ch132/150 继续推进

## 背景

Task 192 在 Ch129 clean 后，使用 Task 191 harness 继续执行：

```powershell
$env:LLM_MODEL='deepseek/deepseek-v4-flash'
powershell -File scripts/run_with_timeout.ps1 -TimeoutSec 14400 -- python scripts/run_v10_ch200_climb.py --to 150 --genre xuanhuan
```

Ch130 已 accepted；Ch131 在 ReviewMerger 需要修订后，LiteraryAuditor 调用失败，LLM 返回无法解析为 JSON，导致 chapter failed。由于 harness isolate 模式继续，run 短暂进入 Ch132。发现后已人工中断 wrapper，并将 run 标记为 paused。

## 冻结现场

- DB：`.tmp/task_v10_xuanhuan_ch200.db`
- project_id：`d160a55a51de4a2bb82440ebc03ec23a`
- run_id：`run-v10-xuanhuan-3b4ba8e4`
- 冻结目录：`.tmp/backups/192ah_xuanhuan_ch131_literary_auditor_json_parse_20260726-1511/`
- wrapper log：`logs/wrapper/run-20260726-145944926-20260726-145944926.out.log`
- frozen DB SHA256：`4A128DEBFB66631F84446B0889FEDE7BA2B6973CA21F9E18B8A91A4F7A308FAC`

冻结后 run 状态：

```json
{
  "current_chapter": 131,
  "status": "paused",
  "completed_count": 130,
  "failed_chapters": [131],
  "total_cost": 5.8667
}
```

Head 状态：

- Ch129 accepted：`fix-129-d8015e35`
- Ch130 accepted：`v-7561da67`
- Ch131 current：`v-131-1-2d72805f`，accepted=null
- Ch132 未写入 chapter version（已在 GoalPlanner 阶段中断）

## 失败证据

关键日志：

```text
llm.parse_failed agent=literary_auditor chapter_number=131 stage=all_fallbacks_exhausted
literary_auditor_node.audit_failed chapter_number=131 error='LLM 返回内容无法解析为 JSON（标准解析和 repair 均失败）'
project_pipeline.chapter_failed chapter_number=131 error='Literary audit failed: LLM 返回内容无法解析为 JSON（标准解析和 repair 均失败）'
```

Ch131 draft：

- version：`v-131-1-2d72805f`
- word_count：3579
- accepted：null

## 修复要求

1. 不得继续 Ch132/150，直到 Ch131 accepted head 与 settlement/summary 完整恢复。
2. 优先使用现有 pipeline API 或安全单章 runner 重跑 Ch131；不得用 Ch150 长跑绕过。
3. 修复后必须确保：
   - Ch131 `accepted_version_id` 非空；
   - Ch131 settlement valid；
   - Ch131 summary 已生成；
   - run `completed_chapters` 包含 1..131 且 `failed_chapters=[]`；
   - Ch132 无 accepted 事实，不影响 resume。
4. 复验：
   - 状态一致性检查：gap_1_131=[];
   - segment audit 至少跑 `--up-to 131`，不得出现 hard halt；
   - T9 至少覆盖 Ch1-Ch131，meta/duplicate/timeline=0；
   - 如修改代码，执行 `ruff check src/ tests/` 与风险匹配 pytest。

## 验收清单

- [ ] Ch131 accepted/current head 修复完成。
- [ ] SettlementExtractor 结果 valid，SummaryWriter 已写 Ch131 summary。
- [ ] run state 恢复为一致状态，failed=[]。
- [ ] segment audit / T9 复验通过。
- [ ] `docs/STATUS.md`、`tasks/V10-README.md`、`docs/INDEX.md`、`README.md`、`AGENTS.md` 同步。
- [ ] 提交 git commit。
