# Task 192.q: xuanhuan Ch17 CreativeDirector JSON parse failure repair

> **阶段**: V10.2 / Task 192 xuanhuan clean Ch100 rebuild
> **类型**: structured JSON robustness / clean source rebuild blocker
> **优先级**: P1
> **状态**: ✅ 已完成（DONE：`tasks/192.q-xuanhuan-ch17-creative-director-json-parse-DONE.md`）

---

## Goal

修复或规避 xuanhuan clean Ch100 rebuild 在 Ch17 触发的 CreativeDirector 结构化 JSON 解析失败，使 Task 192 clean Ch100 rebuild 可继续推进且不污染 source 样本。

---

## Trigger

Task 192.p 完成后，重新初始化 xuanhuan clean Ch100 source：

```powershell
$env:TEMPLATE_ID='xuanhuan'
$env:RUN_ID='192'
$env:CHECKPOINTER_MODE='sqlite'
$env:SONGYAN_FORCE_EXIT='1'
$env:SONGYAN_RUN_COST_BUDGET='20.0'
$env:LLM_MODEL='deepseek/deepseek-v4-flash'
powershell -File scripts/run_with_timeout.ps1 -TimeoutSec 14400 -- python scripts/run_172b_ch100_climb.py --to 25
```

运行到 Ch17 时失败：

- run_id: `run-2f42e276`
- project_id: `d160a55a51de4a2bb82440ebc03ec23a`
- DB: `.tmp/task172b_xuanhuan_ch100.db`
- DB SHA256: `06AA9771A49EC48A18C5BB1BA76C14C2299FDA966BF4D3C52B302023488C2AE1`
- run status: `partial`
- completed: Ch1-Ch16
- failed: `[17]`
- total_cost: `1.886869`
- wrapper: `WRAPPER_RESULT=PASS_NORMAL_EXIT`, `EXIT_CODE=0`

直接错误：

```text
creative_director.parse_failed
LLM 返回内容无法解析为 JSON: Expecting ',' delimiter: line 3 column 112 (char 146)
```

raw response 片段显示 JSON 字符串内出现未转义英文引号：

```json
"creative_intent": "......而不是觉得"又过了一关"。"
```

---

## Frozen Evidence

冻结目录：

```text
.tmp/backups/192q_xuanhuan_ch17_creative_director_json_failure_20260725-153447/
```

包含：

- `task172b_xuanhuan_ch100.db`
- `task172b_xuanhuan_project.json`
- `192-xuanhuan-ch100-climb.md`
- `wrapper.out.log`
- `wrapper.err.log`
- `wrapper.meta.txt`
- `command.ps1`

---

## In Scope

- [x] 定位 CreativeDirector 结构化 JSON 解析路径与通用 JSON repair 能力边界。
- [x] 判断是否应在通用 LLM JSON repair 层处理未转义内部引号，或在 CreativeDirector prompt/解析层定点处理。
- [x] 修复后补聚焦测试，覆盖 CreativeDirector raw response 中字符串内部未转义英文引号的场景。
- [x] 保持 CED / T9 / five-gate / segment audit 口径不变。
- [x] 修复后重跑聚焦测试、ruff、scifi 短窗口回归（如结构化解析通用层受影响）。
- [x] 从 Ch17 resume 或必要时 clean rerun，并确认 failed chapter 不继续污染 source。

---

## Out of Scope

- 不放宽 `RUN_ID=192` 的 `ON_FAILURE=abort` 语义。
- 不把当前 failed Ch17 样本作为 clean source 通过证据。
- 不启动 xuanhuan Ch200 或其他体裁 Ch200。
- 不修改优秀度信号、CED、T9 或五门阈值。

---

## Acceptance

- [x] 根因有日志/代码证据。
- [x] 必要代码修复有聚焦测试。
- [x] `python -m pytest tests/ -q` 通过，或若只做定点修复，至少先跑聚焦测试并在父任务收口前补全量。
- [x] `ruff check src/ tests/` 通过。
- [x] xuanhuan Ch17 不再因同类 CreativeDirector JSON parse failure 阻塞。
- [x] 继续 Task 192 前，更新本任务 DONE 文档或在 Task 192 执行记录中登记修复证据。
