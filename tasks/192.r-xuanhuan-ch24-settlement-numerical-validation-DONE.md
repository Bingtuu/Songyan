# Task 192.r: xuanhuan Ch24 settlement numerical validation repair — DONE

> **阶段**: V10.2 / Task 192 xuanhuan clean Ch100 rebuild
> **类型**: settlement validation failure / clean source rebuild blocker
> **优先级**: P1
> **状态**: ✅ 完成
> **日期**: 2026-07-25

---

## 根因判断

Ch24 首次运行在 SettlementExtractor 数值结算校验处失败：

```text
角色 char-9f6c78ce 的 cultivation_level closing_value (6.5) 不等于 公式值 (4.500)
```

该失败发生在 LLM 结算输出层，非 CED / T9 / five-gate 口径问题。按 Task 192 纪律已冻结现场并停止继续推进。随后从 Ch24 resume，重新生成/结算后未复现该数值不闭合错误，Ch24 accepted，Ch25 继续完成。

本任务未修改代码；处理方式为冻结现场 + rerun 验证，失败样本不作为通过证据。

---

## 冻结现场

| 项 | 值 |
|----|----|
| run_id | `run-2f42e276` |
| project_id | `d160a55a51de4a2bb82440ebc03ec23a` |
| DB SHA256 | `3F997AC71057211012AC559BA72ADE7ACC2684C19E4174C794F4CBCB06A87235` |
| 冻结目录 | `.tmp/backups/192r_xuanhuan_ch24_settlement_numerical_validation_20260725-162532/` |
| wrapper | `WRAPPER_RESULT=PASS_NORMAL_EXIT`, `EXIT_CODE=0` |
| run state | `partial`, completed Ch1-Ch23, failed `[24]` |

---

## 复验

resume 命令：

```powershell
$env:TEMPLATE_ID='xuanhuan'
$env:RUN_ID='192'
$env:CHECKPOINTER_MODE='sqlite'
$env:SONGYAN_FORCE_EXIT='1'
$env:SONGYAN_RUN_COST_BUDGET='20.0'
$env:LLM_MODEL='deepseek/deepseek-v4-flash'
powershell -File scripts/run_with_timeout.ps1 -TimeoutSec 7200 -- python scripts/run_172b_ch100_climb.py --to 25
```

结果：

- wrapper: `logs/wrapper/run-20260725-162600836-20260725-162600836.meta.txt`
- `WRAPPER_RESULT=PASS_NORMAL_EXIT`
- final_status: `completed`
- completed: Ch1-Ch25
- failed: `[]`
- total_cost: `3.120366`

Ch25 报告：

```text
docs/reports/192-xuanhuan-ch100-climb.md
```

关键指标：

- accepted: 25/25
- budget_peak: 0.8632
- before_emerg_peak: 0.0
- emergency: 0
- overdue: 0
- health: 9.1
- CED/1k: 2.0874

---

## 注意事项

Ch25 后 arc summary 生成出现一次非阻塞 parse failure，`trigger_layered_summaries.arc_failed` 被记录，但 `project_pipeline.chapter_success` 与 final_status 均为 completed。该问题不阻塞 Ch25 accepted，但后续若影响 Ch100/Ch200 分层摘要质量，应单独建立后缀修复任务。

---

## 后续

Task 192 可以继续推进到 Ch50 前的下一段，但仍需保持 `RUN_ID=192` 默认 `ON_FAILURE=abort`；任一 failed chapter 必须继续冻结并开后缀修复。
