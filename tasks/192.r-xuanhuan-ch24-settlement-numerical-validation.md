# Task 192.r: xuanhuan Ch24 settlement numerical validation repair

> **阶段**: V10.2 / Task 192 xuanhuan clean Ch100 rebuild
> **类型**: settlement validation failure / clean source rebuild blocker
> **优先级**: P1
> **状态**: ✅ 已完成（DONE：`tasks/192.r-xuanhuan-ch24-settlement-numerical-validation-DONE.md`）

---

## Goal

处理 xuanhuan clean Ch100 rebuild 在 Ch24 触发的 SettlementExtractor 数值结算校验失败，恢复 Task 192 第一段 Ch25 推进，并确保 clean source 不被 failed chapter 污染。

---

## Trigger

Task 192.q 修复后，从 Ch17 resume 到 Ch25：

```powershell
$env:TEMPLATE_ID='xuanhuan'
$env:RUN_ID='192'
$env:CHECKPOINTER_MODE='sqlite'
$env:SONGYAN_FORCE_EXIT='1'
$env:SONGYAN_RUN_COST_BUDGET='20.0'
$env:LLM_MODEL='deepseek/deepseek-v4-flash'
powershell -File scripts/run_with_timeout.ps1 -TimeoutSec 10800 -- python scripts/run_172b_ch100_climb.py --to 25
```

运行到 Ch24 时失败：

- run_id: `run-2f42e276`
- project_id: `d160a55a51de4a2bb82440ebc03ec23a`
- DB: `.tmp/task172b_xuanhuan_ch100.db`
- DB SHA256: `3F997AC71057211012AC559BA72ADE7ACC2684C19E4174C794F4CBCB06A87235`
- run status: `partial`
- completed: Ch1-Ch23
- failed: `[24]`
- total_cost: `2.766983`
- wrapper: `WRAPPER_RESULT=PASS_NORMAL_EXIT`, `EXIT_CODE=0`

直接错误：

```text
settlement.validation_failed
角色 char-9f6c78ce 的 cultivation_level closing_value (6.5) 不等于 公式值 (4.500)
settlement_extractor_node.validation_failed_needs_review
```

---

## Frozen Evidence

冻结目录：

```text
.tmp/backups/192r_xuanhuan_ch24_settlement_numerical_validation_20260725-162532/
```

包含：

- `task172b_xuanhuan_ch100.db`
- `task172b_xuanhuan_project.json`
- `192-xuanhuan-ch100-climb.md`
- `wrapper.out.log`
- `wrapper.err.log`
- `wrapper.meta.txt`
- `command.ps1`

---

## In Scope

- [x] 读取冻结 DB/log，确认 Ch24 数值更新内容、公式、正文证据。
- [x] 判断是否为 LLM 瞬时结算错误、数值校验口径缺陷、或 xuanhuan 修炼等级不适合普通 numerical ledger。
- [x] 若为机制缺陷，定点修复并补测试。
- [x] 若为瞬时结算错误，必须 rerun/resume 到 Ch24 成功，并记录当前 failed 样本不能作为通过证据。
- [x] 修复或复跑后确认 project_runs `failed_chapters=[]`，accepted heads 覆盖到目标章节。
- [x] 不修改 CED / T9 / five-gate / segment audit 口径。

---

## Out of Scope

- 不放宽 `RUN_ID=192` 的 `ON_FAILURE=abort`。
- 不把 failed Ch24 样本作为 clean source 通过证据。
- 不启动 Ch200。
- 不修改优秀度信号。

---

## Acceptance

- [x] 根因有日志/DB 证据。
- [x] 若改代码，有聚焦测试与 ruff。
- [x] Ch24 不再以同类 settlement numerical validation failure 阻断。
- [x] 第一段 Ch25 可继续推进；若仍失败，冻结并继续后缀修复。
- [x] 继续 Task 192 前，更新本任务 DONE 文档或在 Task 192 执行记录中登记修复证据。
