# Task 193.p: wuxia Ch125 missing checkpoints table

> **阶段**: V10.2 Task 193 wuxia Ch200 climb
> **类型**: harness/runtime compatibility repair
> **优先级**: P0
> **状态**: ✅ 已完成；DONE：`tasks/193.p-wuxia-ch125-missing-checkpoints-table-DONE.md`
> **触发时间**: 2026-07-28

---

## 触发

Task 193 已完成 wuxia Ch28 deterministic clean，并通过 Task 191 harness 初始化 V10 Ch200 target DB。随后执行第一段：

```powershell
powershell -File scripts/run_with_timeout.ps1 -TimeoutSec 2400 -- python scripts/run_v10_ch200_climb.py --to 125 --genre wuxia
```

在进入 Ch101 前失败：

```text
sqlite3.OperationalError: no such table: checkpoints
WRAPPER_RESULT=FAIL_NONZERO_EXIT
```

---

## 现场冻结

| 项 | 值 |
|----|----|
| freeze dir | `.tmp/backups/193p_wuxia_ch125_missing_checkpoints_table_20260728-0540/` |
| wrapper | `run-20260728-053816590-20260728-053816590` |
| target DB | `.tmp/task_v10_wuxia_ch200.db` |
| target DB SHA256 | `DE620366E10DDA74AFBA1B346B81029BC32E916F63969960F41B45AC2B08C258` |
| run_id | `run-v10-wuxia-5bbfab3a` |
| run state | `status=running`、current_chapter=101、completed=1..100、failed=[] |
| accepted heads | 100 |
| missing tables | `checkpoints`、`writes` |

---

## 根因判断

Task 191 harness 从旧 Ch100 source DB 复制出 V10 target DB；该旧 DB 尚未包含 LangGraph sqlite checkpointer tables。`run_project_pipeline()` 在编译 graph 前会调用 `prune_orphan_checkpoints()`，该函数直接 `DELETE FROM checkpoints`，导致旧 DB continuation 在 checkpointer setup 前崩溃。

---

## 修复边界

- `prune_orphan_checkpoints()` 在 sqlite 模式下应先检测 `checkpoints` 表。
- 表不存在时幂等返回 0，不创建业务写入，不改变 Ch200 判据。
- 保留已有 cleanup 行为：表存在时继续按 `metadata.project_id` 清理孤儿 checkpoint。
- 补测试覆盖 sqlite 模式但 checkpoint tables 尚不存在的旧 DB continuation 场景。

---

## 验证要求

- 聚焦测试：`python -m pytest tests/test_153_run_level_resume.py::TestCheckpointPruning -q`
- ruff：`ruff check src/songyan/workflows/checkpointer.py tests/test_153_run_level_resume.py`
- resume Task 193 Ch125 分段，不得重建 source 或绕过 Task 191 harness。

---

## 修复结果

已完成。`prune_orphan_checkpoints()` 在 sqlite 模式下会先检测 `checkpoints` 表；旧 continuation DB 尚无 LangGraph checkpoint tables 时幂等返回 0。Task 193 Ch125 resume 已越过原崩溃点并推进到 Ch114 accepted；后续 timeout 均为 wrapper 时间上限，不是 missing table 复发。
