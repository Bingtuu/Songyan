# Task 193.w DONE: 段审计判定消费修复（segment audit verdict 解析 + stale health 防护）

> **阶段**: V10.2 Task 193 wuxia Ch200 climb 前置口径修复
> **类型**: 评测工具判定消费修复（未触碰生成链路）
> **状态**: ✅ 已完成

---

## 修复结果

### F1 · five-gate health 报告章号透明化

- `src/songyan/evals/five_gate_acceptance.py`：
  - `TargetMetrics` 新增 `health_report_chapter: int | None`，health 查询同时取 `checked_up_to_chapter`（`:372-388`）；
  - health 门 detail 携带章号：`latest health must be >= 8.0 (report @Ch123)`，无报告时为 `(no continuity report)`；
  - `metrics.to_dict()` 输出同名字段；文本报告 health 行附 `(report @ChX)`。
- 判定逻辑零变化（阈值/比较符/门结构均未动）。

### F2 · harness 段审计 verdict 块

- `scripts/run_v10_ch200_climb.py::run_audit`：解析 five_gate 与 segment_audit 的 JSON 输出（segment_audit 退出码恒 0 的 observe 哲学保留，判定改由消费侧解析），payload 新增 `verdict` 块：
  `five_gate` / `segment_halt_would_fire` / `critical_orphans` / `health_report_chapter` / `stale_health_warning`（+ 条件性 `health_note`）。

### F3 · stale health 预警（192.aw 型）

- `stale_health_warning = up_to - health_report_chapter >= 2`；为 true 时 `health_note` 提示"先补跑 continuity audit 到 Ch<up_to> 再判 health 门"。仅预警不阻塞，评测工具不产生写副作用。

## 验证证据

| 验证 | 结果 |
|------|------|
| `tests/test_193w_segment_audit_verdict.py` | **9 passed**（先 RED 后 GREEN：章号上浮/多报告取最新/无报告 None/门 detail 章号/缺报告标记；verdict 块完整/stale 预警/segment halt 上浮/lag 边界） |
| 相邻套件（test_182 five-gate 工具 / test_191 harness / test_193r） | **59 passed** |
| wuxia 实库 five-gate @125（显式 189 baseline） | **PASS**；health 门 detail=`... (report @Ch123)`、`health_report_chapter=123`（任务书验收点） |
| xuanhuan 冻结库 five-gate @200 | **PASS**，halt=None、overdue=14、health=8.1、`health_report_chapter=200`——判定结论与 192 DONE 完全一致 |
| `ruff check src/ tests/ scripts/` | **All checks passed** |
| `mypy src/` | **Success: no issues found in 176 source files** |
| 全量 `python -m pytest tests/ -q` | **3062 passed, 2 skipped, 1 xfailed**（770s） |

## 备注

- 本任务不改五门判定函数与阈值、不改 segment_audit 检查逻辑与退出码语义。
- Ch150 段边界审计起，harness `--audit` payload 自带 verdict 块，段审计判定不再依赖人工读 JSON；health 门 FAIL 时先看 `stale_health_warning`。

## 验收对照

- [x] five-gate JSON/文本输出含 `health_report_chapter`；Ch125 实库复跑显示 @Ch123。
- [x] harness `--audit` payload 含完整 verdict 块；`halt_would_fire=true` fixture 正确上浮。
- [x] 192.aw 场景回归：报告滞后 fixture 触发 `stale_health_warning=true` 且带补跑提示。
- [x] 新增测试全绿；全量 pytest 与 ruff 通过。
- [x] xuanhuan 冻结库 five-gate @200 复跑结论与 192 DONE 一致。
- [x] 产出本 DONE 文档，同步 `docs/STATUS.md`、`tasks/V10-README.md`、`README.md`。
