# Task 193.z: wuxia Ch155 LLMAuditor JSON parse failure

> **阶段**: V10.2 Task 193 wuxia Ch200 climb blocker
> **类型**: Ch155 单章恢复 / run state cleanup / 段跑冻结
> **优先级**: P0
> **依赖**: Task 193.y；Ch150 段边界 PASS 后 Ch151→Ch175 段跑
> **状态**: ✅ 已完成；post-fix segment blocker 已路由并由 193.aa 修复

---

## 背景

Task 193 从 Ch150 PASS 状态继续推进到 Ch175：

```powershell
$env:LLM_MODEL='deepseek/deepseek-v4-flash'
powershell -File scripts/run_with_timeout.ps1 -TimeoutSec 21600 -- python scripts/run_v10_ch200_climb.py --to 175 --genre wuxia --cost-budget 14
```

Ch151、Ch152、Ch153、Ch154 均完成 accepted；Ch155 在二轮修订后进入 LLMAuditor 时，LLM 返回空内容，JSON 标准解析和 repair 均失败。pipeline 以 isolate 方式记录 `chapter_failed` 后继续进入 Ch156；为避免扩大 gap 与继续消耗成本，已人工中止外部进程，并用 `ProjectRunRepository.update()` 将 run 冻结为 paused。

---

## 冻结现场

- DB：`.tmp/task_v10_wuxia_ch200.db`
- project_id：`273a8408be8e4caf8cbc1e91954da600`
- run_id：`run-v10-wuxia-5bbfab3a`
- wrapper：`logs/wrapper/run-20260729-213157512-20260729-213157512.out.log`
- 冻结目录：`.tmp/backups/193z_wuxia_ch155_llm_auditor_json_parse_20260729-220836/`
- 冻结后 accepted：154/154，Ch154 accepted/current head 后续以 DB 为准
- 冻结后 run state：

```json
{
  "status": "paused",
  "pause_reason": "user_requested",
  "current_chapter": 155,
  "completed_count": 154,
  "failed_chapters": [155],
  "total_cost": 8.796384
}
```

---

## 失败证据

关键日志：

```text
llm.parse_failed agent=llm_auditor chapter_number=155 raw_preview= stage=all_fallbacks_exhausted
llm_auditor.parse_failed agent=llm_auditor chapter_number=155 raw_response= response_length=0
llm_auditor_node.audit_failed chapter_number=155 error='LLM 返回内容无法解析为 JSON（标准解析和 repair 均失败）' version_id=rev-155-3-9ac1de69
project_pipeline.chapter_failed chapter_number=155 error='LLM audit failed: LLM 返回内容无法解析为 JSON（标准解析和 repair 均失败）' version_id=rev-155-3-9ac1de69
```

Ch155 已生成并修订到候选版本：

- initial draft：`v-155-1-59635796`
- revision 1：`rev-155-2-2c155140`
- revision 2 / failure version：`rev-155-3-9ac1de69`

`rev-155-3-9ac1de69` 的 RuleAuditor 结果显示：

```text
ai_tell_count=0
text_artifact_count=0
mandatory_reference_check_passed=True
word_count_ok=True
```

因此当前 blocker 不是正文 T9 或 mandatory reference 未消费，而是 LLMAuditor 二轮 JSON parse failure 导致 Ch155 未进入 human_gate / settlement / summary / accepted head。

---

## 修复要求

1. 不得继续 Ch156+，直到 Ch155 accepted head、settlement、summary 与 run state 恢复一致。
2. 优先基于 `rev-155-3-9ac1de69` 做单章 resume 或审查重试；不得覆盖旧 `chapter_versions`。
3. 修复后必须确保：
   - Ch155 `accepted_version_id` 非空；
   - Ch155 settlement valid；
   - Ch155 summary 已生成；
   - run `completed_chapters` 包含 1..155；
   - run `failed_chapters=[]`；
   - run status 可继续 resume；
   - Ch156 非 accepted 草稿不得作为事实源。
4. 复验至少执行：

```powershell
python scripts/run_v10_ch200_climb.py --status --genre wuxia --format json
python scripts/five_gate_check.py --genre wuxia --db .tmp/task_v10_wuxia_ch200.db --project-id 273a8408be8e4caf8cbc1e91954da600 --up-to 155 --baseline tasks/189-scifi-ch200-baseline.json --format json
python scripts/segment_audit.py --db .tmp/task_v10_wuxia_ch200.db --project-id 273a8408be8e4caf8cbc1e91954da600 --up-to 155 --genre wuxia --format json
```

5. 若修复过程修改代码，必须执行：

```powershell
powershell -File scripts/run_with_timeout.ps1 -TimeoutSec 2400 -- python -m pytest tests/ -q
ruff check src/ tests/
git diff --check
```

纯 DB 状态修复或单章 resume 不需要全量 pytest，但必须记录风险匹配验证证据。

---

## 验收清单

- [x] Ch155 accepted/current head 修复完成（193.z 单章恢复后 `v-3af05880`；193.aa 后 final head `fix-155-segment-193aa`）。
- [x] SettlementExtractor valid，SummaryWriter 已写 Ch155 summary。
- [x] run state 恢复为 completed 1..155、failed=[]、status 可 resume。
- [x] Ch156 非 accepted 草稿已记录且不作为事实源。
- [x] Ch155/segment boundary 相关 status/audit 复验通过（post-fix segment blocker 已由 193.aa 修复到 PASS）。
- [x] 产出 DONE 文档并同步 `docs/STATUS.md`、`tasks/V10-README.md`、`AGENTS.md`。
- [ ] 提交 git commit，不 push。

---

## 完成记录

见 `tasks/193.z-wuxia-ch155-llm-auditor-json-parse-DONE.md`。193.z 的 parse failure 已通过 Ch155 单章 resume 修复；后续 segment critical orphan 已路由并由 `tasks/193.aa-wuxia-ch155-segment-critical-orphan-DONE.md` 修复。当前可回到 Task 193 主线，从 Ch156 继续推进到 Ch175。
