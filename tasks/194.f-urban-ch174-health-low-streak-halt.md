# Task 194.f: urban Ch174 health_low_streak_halt

> **父任务**: Task 194 urban Ch200 爬坡  
> **类型**: health hard gate / Ch172-Ch174 low-health streak / checkpoint pre-Ch175 freeze  
> **状态**: ◐ 进行中  
> **冻结时间**: 2026-07-30 15:01  

---

## 背景

Task 194 在 194.e 修复 Ch162 GoalPlanner JSON parse failure 后，继续使用 Task 191 harness：

```powershell
powershell -File scripts/run_with_timeout.ps1 -TimeoutSec 21600 -- python scripts/run_v10_ch200_climb.py --to 175 --genre urban --cost-budget 16 --format json
```

wrapper：

```text
logs/wrapper/run-20260730-133054374-20260730-133054374.out.log
```

Ch162-Ch174 已 accepted；但在 Ch174 后触发自动硬门：

```text
AutoHaltException: 连续 health_low 触发候选硬门禁（Ch172-Ch174）:
['health_low_streak_halt: window=3 P2_total=3 >= limit=2']
```

这不是 cost budget 暂停，而是质量硬门。修复前不得继续 Ch175。

---

## 冻结现场

冻结目录：

```text
.tmp/backups/194f_urban_ch174_health_low_streak_halt_20260730-1501/
```

包含：

- target DB copy：`task_v10_urban_ch200.db`
- wrapper log：`wrapper.out.log` / `wrapper.err.log`
- wrapper meta：`wrapper.meta.txt`
- harness status：`status.json`
- DB/run/head snapshot：`run_state.json`
- DB SHA256：`B5537FBDDADEC9B92C28D1EA9297F7CC6B0882C3BFDEC141DDFC1E5EF27C9DE5`

冻结后的 run state：

- run_id：`run-v10-urban-743a979a`
- status：`paused`
- pause_reason：`auto_halt:health_low_streak_halt`
- current_chapter：174
- completed：1..174
- failed_chapters：`[]`
- total_cost：10.554465

Accepted heads：

| Chapter | accepted/current head |
|---------|-----------------------|
| Ch172 | `v-bad824d1` |
| Ch173 | `v-f52c35c6` |
| Ch174 | `v-f5a8d2d8` |

Latest continuity report evidence:

| up_to | report_id | health |
|-------|-----------|--------|
| 174 | `cont_b8daaae4` | 7.9 |

---

## 修复目标

1. 定位 Ch172-Ch174 health_low streak 的 P2 来源，尤其是 overdue/orphan/suggested human marks。
2. 通过最小可追溯修复解除 streak：
   - 优先刷新/resolve 可证据化 tracking；
   - 必要时创建版本化 patch，不覆盖正文；
   - 不修改 health 判据或硬门阈值。
3. 修复后复跑 continuity / health 相关检查，确认不再触发 `health_low_streak_halt`。
4. 恢复主线：
   - run completed 保持 1..174；
   - failed=[]；
   - resume 到 Ch175；
   - Ch175 必须执行 checkpoint audit（显式 Task 189 baseline、five-gate、segment、T9）。

---

## 约束

- 不继续 Ch175 生成，直到本任务修复完成并出 DONE。
- 不把优秀度或结构 spike 混入本修复。
- SQLite 仍是唯一长期事实源；修复写入走 repository/service/UnitOfWork 或既有工具。
- 每次正文修复必须创建新的 `chapter_versions` 记录，禁止覆盖。
