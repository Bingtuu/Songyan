# Task 194.h: urban Ch179 settlement numerical validation

> **父任务**: Task 194 urban Ch200 爬坡  
> **类型**: SettlementExtractor numerical validation / accepted gap freeze  
> **状态**: ◐ 进行中  
> **冻结时间**: 2026-07-30 18:13  

---

## 背景

Task 194 在 Ch175 checkpoint PASS 后，使用 Task 191 harness 继续推进：

```powershell
powershell -File scripts/run_with_timeout.ps1 -TimeoutSec 21600 -- python scripts/run_v10_ch200_climb.py --to 200 --genre urban --cost-budget 20 --format json
```

Ch176-Ch178 已 accepted：

| Chapter | accepted/current head |
|---------|-----------------------|
| Ch176 | `v-9e8700c1` |
| Ch177 | `v-4fccf48d` |
| Ch178 | `v-424077ac` |

Ch179 质量门通过后进入 SettlementExtractor，但 settlement numerical validation 失败：

```text
角色 char-b48d0e1f 的 heartbeat_interval_ms closing_value (480.0) 不等于 公式值 (362.000)
```

因此 Ch179 未 accepted，run 出现 failed chapter。pipeline 随后继续进入 Ch180，已人工中断并冻结，避免继续形成 accepted gap。

---

## 冻结现场

冻结目录：

```text
.tmp/backups/194h_urban_ch179_settlement_numerical_validation_20260730-1813/
```

包含：

- target DB copy：`task_v10_urban_ch200.db`
- wrapper log：`wrapper.out.log` / `wrapper.err.log`
- harness status：`status.json`
- run/head snapshot：`run_state.json`
- DB SHA256：`db.sha256.txt`

说明：本次 wrapper 被人工中断，未产生 `wrapper.meta.txt`。

冻结后的 run state 使用 `ProjectRunRepository.update()` 写回：

- run_id：`run-v10-urban-743a979a`
- status：`paused`
- pause_reason：`manual_freeze:ch179_settlement_numerical_validation`
- current_chapter：179
- completed：1..178
- failed：`[179]`
- total_cost：11.259048

Current heads：

| Chapter | current | accepted | status |
|---------|---------|----------|--------|
| Ch179 | `v-179-1-2d4577e0` | null | draft |
| Ch180 | `rev-180-3-088c44ff` | null | under_review |

Ch180 为中断前的 transient current，不得继续使用；修复必须回到 Ch179。

---

## 修复目标

1. 修复 Ch179 settlement numerical validation failure。
2. 使 Ch179 形成 accepted/current head，并完成 SettlementExtractor / SummaryWriter。
3. 恢复 run state：
   - completed includes 1..179；
   - failed=[]；
   - current_chapter 至少 179；
   - 无 accepted gap。
4. 回到 Task 194 主线继续 Ch180→Ch200，真实 `--to` 必须带 cost budget。

---

## 约束

- 修复完成前不得继续 Ch180+。
- 不直接覆盖正文；需要正文变更时必须新增 `chapter_versions`。
- 不修改 numerical validation 规则；如需修复，只修复 settlement/证据/正文事实。
- 不混入优秀度或结构 spike。
