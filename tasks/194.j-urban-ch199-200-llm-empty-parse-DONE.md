# Task 194.j DONE: urban Ch199-Ch200 LLM empty parse / accepted gap

> **父任务**: Task 194 urban Ch200 爬坡
> **状态**: ✅ 完成
> **完成时间**: 2026-07-31

## 结论

Ch199/Ch200 accepted gap 已清除，但本修复登记为 fallback model 样本：`deepseek/deepseek-v4-flash` 在长结构化 prompt 下多次返回空响应，`--on-failure retry` 生效后仍不能稳定恢复；随后临时显式使用 `deepseek/deepseek-chat` 完成 Ch199/Ch200。

## 证据

- harness 增强：`scripts/run_v10_ch200_climb.py` 新增默认不变的 `--on-failure {isolate,retry,abort}`。
- 聚焦验证：`python -m pytest tests/test_191_ch200_harness.py -q` → 11 passed。
- lint：`ruff check scripts/run_v10_ch200_climb.py tests/test_191_ch200_harness.py` → passed。
- flash retry 仍失败：`.tmp/backups/194j_retry_on_failure_still_failed_20260731-1504/`。
- fallback 后 Ch199 accepted/current head=`v-6635ac72`，word_count=2884。
- fallback 后 Ch200 accepted/current head=`v-b687fc47`，word_count=2805。
- run 恢复到 completed=1..200、failed=[]、total_cost=14.91622。

## 风险登记

- 194.j 的生成链路恢复依赖 `deepseek/deepseek-chat`，不是 flash clean sample。
- scifi short-window regression 曾在 flash 下复现 CreativeDirector empty response，说明该问题是 provider/长 prompt 稳定性风险，不是 urban DB 独有问题。
- fallback 仅用于完成 V10.2 urban Ch200 主线样本，不能据此声称 flash 已恢复。

## 后续

Ch200 终点审计随后暴露 T9 meta=1，已按规程冻结并路由 194.k。
