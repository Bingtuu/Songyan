# Task 194.j: urban Ch199-Ch200 LLM empty parse / accepted gap

> **父任务**: Task 194 urban Ch200 爬坡  
> **类型**: LLM empty response / accepted gap repair  
> **优先级**: P0  
> **状态**: ✅ 完成；DONE：`tasks/194.j-urban-ch199-200-llm-empty-parse-DONE.md`
> **创建时间**: 2026-07-31  

## 背景

194.i DONE 后，Task 191 harness 使用真实命令继续 Ch199→Ch200：

```powershell
powershell -File scripts/run_with_timeout.ps1 -TimeoutSec 7200 -- python scripts/run_v10_ch200_climb.py --to 200 --genre urban --cost-budget 20 --format json
```

wrapper 正常退出，但 run 结果为 `partial`，不是验收通过：

- wrapper out: `logs/wrapper/run-20260731-121733537-20260731-121733537.out.log`
- wrapper meta: `logs/wrapper/run-20260731-121733537-20260731-121733537.meta.txt`
- `WRAPPER_RESULT=PASS_NORMAL_EXIT`
- final_status=`partial`
- completed=1..198
- failed=[199, 200]
- total_cost=14.43615

冻结备份：

```text
.tmp/backups/194j_urban_ch199_200_llm_empty_parse_20260731-1225/
```

## 失败证据

Ch199:

- Writer 返回 0 字，`scenes_count=0`、`word_count=0`。
- current draft=`v-199-1-8a64d8c8`
- accepted=null
- LiteraryAuditor raw_response 为空，触发 `LLM 返回内容无法解析为 JSON（标准解析和 repair 均失败）`。

Ch200:

- GoalPlanner raw_response 为空。
- 触发 `GoalPlanner LLM call failed: LLM 返回内容无法解析为 JSON（标准解析和 repair 均失败）`。
- 无 chapter head。

Run 已人工冻结：

- status=`paused`
- pause_reason=`manual_freeze:ch199_200_llm_empty_parse`
- current_chapter=199
- completed=1..198
- failed=[199, 200]
- total_cost=14.43615

## Scope

必须完成：

- 清理 Ch199/Ch200 failed gap。
- 使用 Task 191 harness resume，真实 `--to 200` 必须带 `--cost-budget`。
- Ch199/Ch200 均 accepted 后，确认 completed=1..200、failed=[]。
- 执行 Ch200 终点审计，显式使用 `tasks/189-scifi-ch200-baseline.json`。
- 若 Ch200 five-gate/segment/T9 任一失败，继续按 194 后缀修复，不进入 Task 195。
- 创建 DONE 文档后回 Task 194 主线收口。

## Out of Scope

- 不修改 LLM client、prompt 或评测阈值。
- 不把优秀度或结构 spike 混入本修复。
- 不直接覆盖 Ch199 draft；如果需要修正文稿，必须创建新 `chapter_versions`。

## 验收标准

- Ch199 accepted head 非空。
- Ch200 accepted head 非空。
- run completed=1..200，failed=[]。
- Ch200 five-gate PASS。
- Ch200 segment audit PASS。
- Ch200 T9=0。

## 追加记录（2026-07-31）

为避免 LLM 空响应被 `isolate` 直接推进成 accepted gap，已给 Task 191 harness 增加默认不变的失败策略参数：

```powershell
python scripts/run_v10_ch200_climb.py --to 200 --genre urban --cost-budget 20 --on-failure retry --format json
```

验证：

- `python -m pytest tests/test_191_ch200_harness.py -q` → 11 passed
- `ruff check scripts/run_v10_ch200_climb.py tests/test_191_ch200_harness.py` → passed

实际 retry 结果：

- wrapper `run-20260731-144318657`：`--on-failure retry` 生效，Ch199 first attempt 进入正文生成，但 Writer 返回 0 字；自动 retry 过程中出现 GoalPlanner 空响应，最终 failed=[199]。
- wrapper `run-20260731-150024943`：Ch199 first attempt GoalPlanner 空响应，自动 retry 后仍 GoalPlanner 空响应，最终 failed=[199]。

最新冻结：

- status=`paused`
- pause_reason=`manual_freeze:ch199_llm_empty_parse_retry_failed`
- current_chapter=199
- completed=1..198
- failed=[199]
- total_cost=14.666101
- latest backup: `.tmp/backups/194j_retry_on_failure_still_failed_20260731-1504/`

当前判断：194.j 已从单次 accepted gap 修复升级为持续 LLM 空响应问题。下一步不得继续盲跑；需要选择明确修复策略（如更强 LLM retry 机制、临时备用模型并登记样本污染风险、或等待外部 LLM 恢复后再跑）。
